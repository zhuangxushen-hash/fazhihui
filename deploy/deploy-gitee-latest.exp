set timeout 300
set password "s\`3}2KG~n@D?N\$P"

# 在服务器上从 Gitee 拉取最新 test 分支代码
spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui && echo "===FETCH_GITEE===" && git fetch gitee test 2>&1 && echo "===RESET_TO_GITEE===" && git reset --hard gitee/test 2>&1 && echo "===LATEST_COMMIT===" && git log --oneline -5 && echo "===CURRENT_BRANCH===" && git branch -a}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
