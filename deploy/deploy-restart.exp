set timeout 120
set password "s\`3}2KG~n@D?N\$P"

spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui && echo "===RESTART_PROD===" && pm2 start backend/dist/main.js --name prod-backend 2>&1 || pm2 restart prod-backend 2>&1 && echo "===RESTART_TEST===" && pm2 start backend/dist/main.js --name test-backend 2>&1 || pm2 restart test-backend 2>&1 && echo "===PM2_LIST===" && pm2 list --no-color 2>/dev/null}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
