set timeout 300
set password "s\`3}2KG~n@D?N\$P"

# 申请 SSL 证书并配置 Nginx 重定向
spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu && echo "===CERTBOT===" && sudo certbot --nginx -d fazhihui.meichuangmenye.com -d test.meichuangmenye.com --non-interactive --agree-tos -m admin@meichuangmenye.com --redirect 2>&1 && echo "===CERTS===" && sudo certbot certificates 2>&1 | head -20}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT"
        exit 1
    }
}

expect eof
