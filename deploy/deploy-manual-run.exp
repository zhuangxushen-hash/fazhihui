set timeout 30
set password "s\`3}2KG~n@D?N\$P"

spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui/backend && echo "===CHECK_DB===" && ls -la *.sqlite 2>&1 && echo "===RUN_MANUAL===" && timeout 8 node dist/main.js 2>&1 || echo "PROCESS_EXITED"}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
