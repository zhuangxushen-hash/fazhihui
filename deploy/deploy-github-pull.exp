set timeout 300
set password "s\`3}2KG~n@D?N\$P"

# 从 GitHub 拉取最新 test 分支代码
spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui && echo "===FETCH_GITHUB===" && git fetch github test 2>&1 && echo "===RESET_TO_GITHUB===" && git reset --hard github/test 2>&1 && echo "===LATEST_COMMIT===" && git log --oneline -3}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
