set timeout 30
set password "s\`3}2KG~n@D?N\$P"

spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {echo "===CHECK_ENV===" && ls -la /home/ubuntu/fazhihui/backend/.env* 2>&1 && echo "===ENV_CONTENT===" && cat /home/ubuntu/fazhihui/backend/.env 2>&1 || echo "NO_ENV_FILE"}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
