set timeout 300
set password "s\`3}2KG~n@D?N\$P"

# 检查服务器上的依赖问题并修复
spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui && echo "===CHECK_BACKEND_DEPS===" && cd backend && cat package.json | grep helmet && echo "===INSTALL_BACKEND_DEPS===" && npm install 2>&1 | tail -5 && echo "===BUILD_BACKEND===" && npm run build 2>&1 | tail -10}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
