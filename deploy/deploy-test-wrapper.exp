set timeout 30
set password "s\`3}2KG~n@D?N\$P"

spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui && echo "===CHECK_WRAPPER===" && cat start-server.js && echo "===PM2_LOGS===" && tail -5 /home/ubuntu/.pm2/logs/prod-backend-error.log 2>&1 && echo "===TEST_ENV===" && NODE_ENV=production PORT=3001 timeout 8 node start-server.js 2>&1 || echo "EXIT_CODE: $?"}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
