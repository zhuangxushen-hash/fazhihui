set timeout 300
set password "s\`3}2KG~n@D?N\$P"

# 构建前端并验证后端
spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 {cd /home/ubuntu/fazhihui && echo "===BUILD_FRONTEND===" && cd frontend && npm run build 2>&1 | tail -10 && echo "===BACKEND_BUILD_CHECK===" && cd ../backend && ls -f dist/main.js 2>/dev/null && echo "BACKEND_OK" || echo "BACKEND_FAIL"}

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
