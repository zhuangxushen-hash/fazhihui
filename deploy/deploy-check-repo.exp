set timeout 120
set password "s\`3}2KG~n@D?N\$P"

# 检查服务器上的项目目录和远程配置
spawn ssh -o StrictHostKeyChecking=no ubuntu@1.12.57.22 "cd /home/ubuntu/fazhihui && echo '===REMOTES===' && git remote -v && echo '===CURRENT_BRANCH===' && git branch && echo '===LATEST_COMMIT===' && git log --oneline -3"

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
