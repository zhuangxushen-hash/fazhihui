set timeout 300
set password "s\`3}2KG~n@D?N\$P"

# 使用 rsync 同步本地代码到服务器（排除 node_modules, dist, .git）
spawn rsync -avz --exclude='node_modules' --exclude='dist' --exclude='.git' --exclude='*.sqlite' --exclude='stitch_output' /Users/season/AI编程/法律咨询/全链产品/ ubuntu@1.12.57.22:/home/ubuntu/fazhihui/

expect {
    -re "password:|Password:" {
        send "$password\r"
    }
    timeout {
        puts "TIMEOUT waiting for password"
        exit 1
    }
}

expect eof
