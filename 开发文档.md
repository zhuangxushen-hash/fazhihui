# 法智汇 · 开发文档（全链路一体化 SaaS）

> 适用对象：新接手本项目的前端 / 后端 / 全栈开发者  
> 文档目标：让你在 30 分钟内搞清楚「项目是什么、怎么跑起来、代码长什么样、核心业务怎么串、有哪些坑」  
> 文档依据：基于当前代码库静态梳理（含 `file:line` 实证），非凭记忆  
> 最后更新：2026-09-01（本节「功能实体详解」为本版重点新增，逐实体列出：做什么 / 前置条件 / 后置条件 / 关联功能 / 关键代码 / 一期状态）

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构总览](#2-技术架构总览)
3. [目录结构](#3-目录结构)
4. [后端模块总览（47 个模块索引）](#4-后端模块总览)
5. [功能实体详解（逐实体：功能 / 前置 / 后置 / 关联 / 代码 / 一期状态）](#5-功能实体详解)
6. [数据模型要点（状态枚举 + 裸外键风险）](#6-数据模型要点)
7. [核心业务链路](#7-核心业务链路)
8. [前端架构](#8-前端架构)
9. [API 约定](#9-api-约定)
10. [本地开发与运行](#10-本地开发与运行)
11. [配置项说明（backend/.env）](#11-配置项说明)
12. [部署方式（PM2 + Nginx）](#12-部署方式)
13. [多端壳子](#13-多端壳子)
14. [第一期上线范围与菜单收口](#14-第一期上线范围与菜单收口)
15. [已知技术债与待办](#15-已知技术债与待办)
16. [角色与权限模型](#16-角色与权限模型)
17. [附：最短上手路径](#17-附最短上手路径)

---

## 1. 项目概述

**法智汇** 是面向「网推律所」的全链路一体化 SaaS：覆盖 **获客 → 线索 → 谈案 → 发合同 → 案件办理 → 财务分润 → 客户端(C端)回流 → 合规/经营看板** 的完整业务闭环。

- 后端：NestJS 11 + TypeORM + **SQLite (better-sqlite3)**，单体应用，47 个业务模块
- 前端：Vite 6 + React 19 + Ant Design 6，单仓 SPA（B 端管理后台 + C 端 H5）
- 多端：Android / iOS / 微信小程序 均为「原生壳 + WebView 套 H5」，H5 即 `frontend` 的 `/client` 路由
- 第三方：法大大电子签（mock/uat/prod 可切换）、创蓝短信、微信小程序

---

## 2. 技术架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                        多端壳子                                │
│  Android壳 │ iOS壳 │ 微信小程序(web-view) │ 浏览器 H5          │
└───────────────┬──────────────────────────────┬───────────────┘
                │  HTTPS /api                   │
                ▼                               ▼
        ┌──────────────────┐          ┌──────────────────────┐
        │  frontend (SPA)   │          │   backend (NestJS)    │
        │  Vite+React+AntD  │  ──/api──▶│  Controllers/Service  │
        │  React.lazy 路由   │          │  TypeORM → SQLite      │
        └──────────────────┘          └──────────────────────┘
                                             │
                                    ┌────────┴────────┐
                                    ▼                 ▼
                             法大大电子签(mock)    创蓝短信 / 微信
```

关键点：

- 全局路由前缀 `/api`（`backend/src/main.ts:48`）
- 无消息总线 / 无 EventEmitter，模块间靠**直接注入 Service** 协作（含多处 `forwardRef` 防循环依赖）
- 无全局 `ResponseInterceptor`，响应由各 Controller 自行构造；错误走 `HttpException` 子类
- 无数据库 migration，靠 `synchronize`（开发）与启动期手动 ALTER（生产）维护表结构

---

## 3. 目录结构

```
/Users/season/AI编程/法律咨询/全链产品/
├── backend/                 # NestJS 后端
│   ├── src/
│   │   ├── main.ts          # bootstrap、全局守卫/管道/过滤器、CORS、端口
│   │   ├── app.module.ts    # 全局模块注册（47 个业务模块）
│   │   ├── auth/            # JWT 策略、RolesGuard、@Roles 装饰器、登录
│   │   ├── user/            # 用户/组织/角色/权限/菜单（事实上的共享内核）
│   │   ├── lead/ case/ contract/ fadada/ finance/ client/ compliance/
│   │   ├── marketing/ scrm/ ad-platforms/ ...   # 业务模块
│   │   └── seeds/           # 启动自动执行的演示数据
│   ├── fazhihui.sqlite      # 开发库（自动建表 + 自动 seed）
│   └── .env                 # 配置（不入库）
├── frontend/                # React 前端
│   ├── src/
│   │   ├── App.tsx          # 路由表（全 React.lazy 懒加载 + 守卫）
│   │   ├── components/
│   │   │   ├── Layout.tsx   # 菜单 + 角色矩阵（权限核心）
│   │   │   └── BottomNav.tsx# C 端底部导航
│   │   ├── pages/           # B 端页面（150+）
│   │   ├── pages/client/    # C 端页面
│   │   ├── api/             # axios 封装（72 个文件，对应后端模块）
│   │   └── config/ utils/ constants/ types/
│   └── vite.config.ts       # 端口 + /api 代理
├── android-client-shell/ ios-client-shell/ mini-program*/ web-live/  # 多端壳
├── deploy/                  # 部署脚本（install-env / deploy / nginx）
├── docs/                    # 数据流向、外部对接、命名规范、pending-features
└── 网推律所全链路一体化SaaS产品需求文档V1.0.md 等需求文档
```

---

## 4. 后端模块总览

通用模式：

```ts
@Module({
  imports: [TypeOrmModule.forFeature([实体...]), ...其他模块],
  controllers: [XxxController],
  providers: [XxxService],
  exports: [XxxService],
})
```

47 个模块按域分组（详细实体行为见第 5 章）：

| 域           | 模块                                                                                                                                                                       |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 用户/组织/鉴权/系统 | `user` `auth` `audit（审计）` `system` `approval（审批）`                                                                                                                        |
| 线索/营销/获客    | `lead` `marketing` `ad-platforms` `scrm` `social`                                                                                                                        |
| 案件/合同/办案    | `case` `contract` `seal（用印）` `compliance（合规）` `fadada` `due-diligence` `property-preservation` `bid` `document` `archive-volume` `number-rule` `diagram`                 |
| 客户/财务/HR    | `client` `finance` `hr`                                                                                                                                                  |
| AI/知识/协同/工作 | `ai` `knowledge` `law-tool` `task` `worklog` `schedule` `workbench` `internal-project` `update-dynamic` `dashboard` `statistical-analysis`                               |
| 其他能力        | `call-records` `public-opinion` `reconciliation-rules` `fake-live` `order（排序）` `shortcut` `lawyer-center` `mail` `push-rule` `sms（短信）` `comprehensive` `meeting-room`(空) |

> ⚠️ `rbac`/`opinion`/`conflict-check`/`contract-stage` 常被单拎，实际分别落在 `user`/`public-opinion`/`case`(ConflictCheckService)/`contract`(ContractStage 实体) 内，无独立文件夹。

---

## 5. 功能实体详解

> 卡片格式：**功能 / 前置条件 / 后置条件 / 关联功能 / 关键代码 / 一期状态**。  
> 一期状态取值：🔵 保留 / 🟡 软砍（菜单隐藏，代码在，URL 仍可访问）/ 🟠 演示壳（模板拼接或 mock，非真能力）/ ⚫ 死代码（无写入路径/未注册）。

### 5.1 用户 / 组织 / 鉴权 / 系统

#### User（表名：users（用户））`user`

- **功能**：系统唯一人员主数据，承载账号、密码（bcrypt）、角色枚举、所属组织/团队、经验值与等级；是所有业务模块的人员引用来源。
- **前置条件**：`organization_id（组织ID）`/`team_id（团队ID）` 须存在（二者均为裸 varchar 外键，无 DB 约束）。
- **后置条件**：被 `auth`、`audit（审计）`、`approval（审批）`、`case`、`lead`、`client` 等通过 `UserService` 查询；组织下挂用户。
- **关联功能**：`UserModule` 导出 `UserService, RoleService, MenuService, NotificationService, PermissionService`；`TeamService/UserProfileService` **未导出**。
- **关键代码**：`user.service.ts:27`(findByPhone) / `:112`(validatePassword) / `:122`(addExperience)；`user.entity.ts:26`(role（角色） 枚举列)
- **一期状态**：🔵 保留（共享内核）

#### Organization（表名：organizations（组织））`user`

- **功能**：律所/组织主数据，是几乎所有业务表 `organization_id（组织ID）` 的租户隔离根。
- **前置条件**：无（seed 建「法智汇律所」）。
- **后置条件**：用户、线索、案件、审批、系统配置均按 `organization_id（组织ID）` 归属。
- **关联功能**：被 `auth`、`approval（审批）`、`system`、各业务模块 import（导入）。
- **关键代码**：`user.service.ts:81`(createOrganization)
- **一期状态**：🔵 保留（多租户根）

#### Role（表名：roles（角色））`user`

- **功能**：RBAC 角色定义，含 `code（编码）`、`permissions（权限）`(自由 JSON 数组)、`organization_id（组织ID）`，用于后台角色管理界面。
- **前置条件**：归属组织。
- **后置条件**：仅自身数据；**未回写到 `users.role` 或守卫**。
- **关联功能**：`UserModule` 导出 `RoleService`；当前无任何模块用其做授权判断。
- **关键代码**：`role.entity.ts:17`(permissions（权限） 自由 JSON)、`role.service.ts:18`
- **一期状态**：🔵 保留（但鉴权层未消费，见 §16）

#### Permission（表名：permissions（权限））`user`

- **功能**：权限点字典（code（编码）、module、type（类型）=read/写），RBAC 权限清单来源。
- **前置条件**：无。
- **后置条件**：仅查询返回；无下游联动。
- **关联功能**：`UserModule` 导出 `PermissionService`；与运行时鉴权脱钩，属「配置孤岛」。
- **关键代码**：`permission.service.ts:31`(findByCodes)
- **一期状态**：🟡 软砍（仅有 CRUD，无消费方）

#### Menu（表名：menus（菜单））`user`

- **功能**：后台导航菜单树，`parent_id（父级ID）` 自引用，`permissions（权限）` 为可见性所需权限 code（编码） 数组，`is_visible` 控制显隐。
- **前置条件**：无。
- **后置条件**：返回菜单树供前端渲染；`permissions（权限）` 字段未被守卫消费。
- **关联功能**：`UserModule` 导出 `MenuService`；被前端菜单接口调用。
- **关键代码**：`menu.service.ts:24`(getMenuTree)
- **一期状态**：🔵 保留（前端菜单树数据源）

#### AuditLog（表名：audit_logs（审计日志））`audit（审计）`

- **功能**：统一操作审计日志（登录、增删改、审批等），记录操作人、资源类型/ID、IP、详情。
- **前置条件**：调用方传入 `user_id（用户ID）`/`action（操作）` 等。
- **后置条件**：仅落库；查询受 `super_admin/org_admin` 限制。
- **关联功能**：`AuditModule` 导出 `AuditService`；被 `auth`(登录)等注入。
- **关键代码**：`audit.service.ts:14`(logAction，异常静默) / `:40`(findAll)
- **一期状态**：🔵 保留 ⚠️ 审计失败不影响主流程（catch 仅 console.error）

#### DeploymentConfig（表名：deployment_configs（部署配置））`system`

- **功能**：部署配置（服务器类型/地址、DB、缓存），运维展示，不触达真实部署。
- **前置条件**：组织。
- **后置条件**：无实际副作用。
- **关键代码**：`deployment-config.service.ts:43`(getActiveConfig)
- **一期状态**：🟠 演示壳（含 `db_host/db_user` 等敏感字段，无加密）

#### BrandConfig（表名：brand_configs（品牌配置））`system`

- **功能**：品牌/主题配置（品牌名、LOGO、主色、登录页、备案号），按组织切换主题。
- **前置条件**：组织。
- **后置条件**：前端登录页/主题读取。
- **关键代码**：`brand-config.service.ts:43`(getActiveBrandConfig)
- **一期状态**：🔵 保留

#### Integration（表名：integrations（集成配置））`system`

- **功能**：第三方对接配置（微信/企微/支付宝/API），存 app_id/secret/webhook。
- **前置条件**：组织。
- **后置条件**：无真实调用。
- **关键代码**：`integration.service.ts:43`(testConnection **永远返回成功**)
- **一期状态**：🟠 演示壳（对接占位，未与 AuthService 微信配置打通）

#### ApprovalRequest（表名：approval_requests（审批申请））`approval（审批）`

- **功能**：审批单主表，记录标题、类型(seal/case/contract/finance/other)、发起人、关联对象 `target_type/target_id`、状态机(pending/approved/rejected/cancelled)、当前步骤。
- **前置条件**：`approvers（审批人）` 非空；发起人已登录。
- **后置条件**：更新自身状态机；**不回写 `target_id（目标ID）` 指向的源实体**（如用印/合同状态）。
- **关联功能**：`ApprovalModule` **未导出 `ApprovalService`**，无其它模块注入触发；仅前端调 `POST /approvals` 发起。
- **关键代码**：`approval.service.ts:21`(create（创建）) / `:63`(approve（审批）)
- **一期状态**：🔵 保留 ⚠️ 审批孤岛，结果未传播到业务实体

#### ApprovalStep（表名：approval_steps（审批步骤））`approval（审批）`

- **功能**：审批单逐步记录（顺序、审批人、结果、意见、时间），支持会签/转批/退回。
- **前置条件**：`request_id` 关联申请单。
- **后置条件**：驱动 `approval_requests.current_step`/状态。
- **关键代码**：`approval.service.ts:73`(approve（审批） 内定位当前步)
- **一期状态**：🔵 保留

#### ApprovalFlow（表名：approval_flows（审批流））`approval（审批）`

- **功能**：审批流模板定义（`steps（步骤）` JSON），本应驱动 `approval_requests（审批申请）` 自动生成步骤。
- **前置条件**：组织。
- **后置条件**：无（未联动）。
- **关键代码**：`approval-flow.entity.ts`（ApprovalService 全程未读取）
- **一期状态**：🟡 软砍（定义表，service 未消费）

#### FormTemplate（表名：form_templates（表单模板））`approval（审批）`

- **功能**：审批单据表单模板（财务提款/支付/报销/成本票等），含字段定义与默认审批角色 JSON。
- **前置条件**：组织。
- **后置条件**：返回模板供前端渲染；校验通过后仍由前端再调 `POST /approvals`。
- **关键代码**：`form-template.service.ts:91`(initDefaultTemplates) / `:151`(createFormApproval 仅校验未真正建单)
- **一期状态**：🔵 保留 ⚠️ 「提交即发起」链路断裂

#### Auth（无独立表，依赖 users（用户） + client_profiles（客户档案） + JWT）`auth`

- **功能**：负责 B 端/C 端登录鉴权、JWT 签发与全局角色守卫。B 端走 `users（用户）`(role（角色） 枚举)，C 端走 `client_profiles（客户档案）`(身份证后8位默认密码或微信快捷登录)，两类身份以 JWT `sub` 区分。
- **前置条件**：依赖 `UserService`、`AuditService`、ClientProfile/Organization 仓库。
- **后置条件**：签发 `access_token`；登录落 `audit_logs（审计日志）`；微信授权自动建客户档案。
- **关联功能**：全局 `APP_GUARD=RolesGuard`(`auth.module.ts:37`)；导出 `AuthService`。
- **关键代码**：`auth.service.ts:48`(login) / `:70`(clientLogin) / `:128`(wxPhoneLogin)
- **一期状态**：🔵 保留 ⚠️ 免鉴权风险（见 §15/§16）

---

### 5.2 线索 / 营销 / 获客

#### Lead（表名：leads（线索））`lead`

- **功能**：营销/广告/转介绍等渠道进入系统的「客户线索」统一载体，承载手机号、来源渠道、案由、跟进状态、转化案件等；是销售全链路（线索→商机→案件）起点。
- **前置条件**：必须带 `organization_id（组织ID）` 与 `phone（电话）`；7 天内同手机号线索直接复用。
- **后置条件**：创建后调 `LeadAssignmentService.matchAndAssign` 自动分配；转化时回写 `case_id（案件ID）`/`conversion_status（转化状态）`。
- **关联功能**：被 `marketing/ConversionService`(forwardRef) 回流状态；被 `ad-platforms/webhook.controller.ts:86` 写入；同模块依赖 `LeadAssignmentService`。
- **关键代码**：`lead.service.ts:22`(create（创建）) / `:128`(createFollowUp) / `:84`(updateStatus)
- **一期状态**：🔵 保留 ⚠️ 大量裸外键列（`assign_sales_id（分配销售ID）`/`case_id（案件ID）` 等无 DB 外键）；分配失败静默

#### LeadPool（表名：lead_pool（线索池））`lead`

- **功能**：超时未跟进或手动释放的线索进入公海，供其他销售领取/管理员分配，盘活线索。
- **前置条件**：线索须已分配(`assign_sales_id（分配销售ID）` 非空)才能回收；领取上限 3 次。
- **后置条件**：回收/领取改写 `Lead.status` 与 `assign_sales_id（分配销售ID）`；领取/分配成功调 `NotificationService.notify`(异常静默)。
- **关联功能**：import（导入） `user/NotificationService`。
- **关键代码**：`lead-pool.service.ts:28`(@Cron handleTimeoutRecycle) / `:273`(takeLead)
- **一期状态**：🔵 保留

#### Opportunity（表名：opportunities（商机））`lead`

- **功能**：线索谈案阶段的「商机」载体，记录谈判人、阶段、报价项、阶段变更日志，最终签约转化为案件(Case（案件）)；连接线索与案件的核心枢纽。
- **前置条件**：线索必须存在且同一线索不可重复建商机；签约前须完成谈案 SOP 强制节点。
- **后置条件**：建商机改写 `Lead.status=NEGOTIATING`；签约时创建 `Case（案件）` 并回写 `Opportunity.case_id`/`Lead.case_id`。
- **关联功能**：创建案件注入 `case/Case`；调用同模块 `TalkSOPService.checkRequiredNodesCompleted`。
- **关键代码**：`opportunity.service.ts:141`(createOpportunity) / `:386`(convertToCase)
- **一期状态**：🔵 保留 ⚠️ `convertToCase` 中 `client_id（客户ID）` 写死 `'pending（待处理）'`（占位风险）

#### FollowUp（表名：follow_ups（跟进））`lead`

- **功能**：线索的跟进动作流水（内容、下次动作、时间），销售过程留痕。
- **前置条件**：需 `lead_id（线索ID）`。
- **后置条件**：联动改写 `Lead.status`/`follow_up_time`。
- **关键代码**：`lead.service.ts:128`(createFollowUp)
- **一期状态**：🔵 保留（纯数据载体）

#### InviteTask（表名：invite_tasks（邀约任务））`lead`

- **功能**：邀约岗对线索发起「到所邀约」任务（电话/微信等），记录结果、录音，到所后自动预建商机。
- **前置条件**：须有线索；操作者角色受限(SALES/ADMIN)。
- **后置条件**：成功邀约改 `Lead.status=INVITING`；到所改 `NEGOTIATING` 并自动建商机草稿；录音上传触发 `ComplianceService.runTalkQualityCheck`；创建时 `NotificationService.notify`。
- **关联功能**：import（导入） `user/NotificationService`、`compliance/ComplianceService`(forwardRef)、同模块 `OpportunityService`。
- **关键代码**：`invite-task.service.ts:33`(createInviteTask) / `:181`(updateTaskStatus)
- **一期状态**：🔵 保留

#### TalkSOP（表名：talk_sops（话术SOP））/ 进度（表名：opportunity_sop_progress）`lead`

- **功能**：按案件类型定义谈案流程模板（信息录入/材料上传/合规确认/签字），跟踪每个商机各节点完成状态，签约前校验强制节点。
- **前置条件**：模板须 `enabled（是否启用）`；节点以 JSON 存 `talk_sops.nodes`。
- **后置条件**：节点完成触发 `ComplianceService.runTalkQualityCheck`(异常静默)。
- **关联功能**：import（导入） `compliance/ComplianceService`(forwardRef)；被 `OpportunityService` 调用。
- **关键代码**：`talk-sop.service.ts:38`(createSOP) / `:453`(checkRequiredNodesCompleted)
- **一期状态**：🔵 保留（强制节点校验为签约前置）

#### LeadAssignment（表名：lead_assignments（线索分配））/ 日志（表名：lead_assignment_logs）`lead`

- **功能**：线索自动分配规则（按地区/案由/负载均衡），命中后改写线索归属并记日志。
- **前置条件**：规则 `enabled（是否启用）=true` 且 `organization_id（组织ID）` 匹配。
- **后置条件**：命中后改写 `Lead.assign_sales_id` 并写 `LeadAssignmentLog`。
- **关联功能**：被 `LeadService.create` 调用。
- **关键代码**：`lead-assignment.service.ts:38`(createRule) / `:73`(matchAndAssign)
- **一期状态**：🔵 保留（线索入库核心链路）

#### HandoverLog（表名：handover_logs（交接记录））`lead`

- **功能**：员工离职/调动时把线索、商机、案件批量交接给接收人，含确认与审计。
- **前置条件**：交接人与接收人不同且均存在。
- **后置条件**：确认后改写 `Lead.assign_sales_id`/`Opportunity.negotiator_id`/`Case.assignee_lawyer_id`；发通知+审计。
- **关联功能**：import（导入） `case/Case`、`user/NotificationService`、`audit/AuditService`。
- **关键代码**：`handover.service.ts:34`(initiateHandover) / `:73`(confirmHandover)
- **一期状态**：🟡 软砍（Phase5+ 资产交接，一期暂不开放）

#### AdAccount（表名：ad_accounts（广告账户））`marketing`

- **功能**：统一管理抖音/百度/腾讯/快手等多平台投放账户、分组、余额、授权令牌。
- **前置条件**：须 `organization_id（组织ID）` 与 `platform（平台）`/`account_id（账号ID）`。
- **后置条件**：每日凌晨 2 点检查余额低于阈值时生成 `AdAccountWarning（广告账户预警）`。
- **关联功能**：`ad-platforms/DataSyncService` 按组织+平台+账户 ID 回写 `balance（余额）`。
- **关键代码**：`ad-account.service.ts:23`(create（创建）) / `:170`(@Cron checkBalanceWarnings)
- **一期状态**：🔵 保留

#### AdPlan（表名：ad_plans（广告计划））/ 日志（表名：ad_plan_logs）`marketing`

- **功能**：投放计划（预算/出价/状态/平台计划 ID/起止），所有变更留操作日志。
- **前置条件**：计划须挂 `account_id（账号ID）`（裸外键列）。
- **后置条件**：每次 CRUD/启停/调预算均落 `AdPlanLog`。
- **关键代码**：`ad-plan.service.ts:20`(create（创建）) / `:304`(recordLog)
- **一期状态**：🔵 保留

#### AdMaterial（表名：ad_materials（广告素材））`marketing`

- **功能**：素材库（图文/视频/脚本），含效果数据、ROI、合规预审状态，支持标签与排行。
- **前置条件**：绑定投放计划前 `compliance_status（合规状态）` 须为 PASSED。
- **后置条件**：合规检查改写素材 `compliance_status（合规状态）`；ROI 由 `ConversionService.refreshDailyMaterialRoi` 维护。
- **关联功能**：`MarketingService` import（导入） `compliance/ComplianceService`；`ConversionService` 按 `material_id` 统计。
- **关键代码**：`material.service.ts:65`(create（创建）) / `:153`(bindToPlan)
- **一期状态**：🔵 保留 ⚠️ `account_id（账号ID）`/`plan_id` 为字符串松耦合裸外键

#### AdAccountWarning（表名：ad_account_warnings（广告账户预警））`marketing`

- **功能**：广告账户余额低于阈值的预警记录，可标记已通知/已解决。
- **前置条件**：账户 `status（状态）=ACTIVE` 才检查。
- **后置条件**：无跨模块联动，仅状态自身流转。
- **关键代码**：`ad-account.service.ts:194`(createOrUpdateWarning)
- **一期状态**：🔵 保留

#### ConversionEvent（表名：conversion_events（转化事件））`marketing`

- **功能**：记录「线索→加微→邀约→签约」四级转化回传，按渠道/账户/计划/素材/关键词多维度归因，驱动 ROI 统计。
- **前置条件**：须 `organization_id（组织ID）` 与 `channel（渠道）`。
- **后置条件**：事件创建后回流 `Lead.status`(异常静默)；T+1 重算素材 ROI；sign 事件自动关联案件回款金额。
- **关联功能**：import（导入） `lead/Lead`、`case/Case`、`lead/LeadService`(forwardRef)；`ad-platforms/webhook.controller.ts:123` 直接写。
- **关键代码**：`conversion.service.ts:88`(createEvent) / `:304`(getRoiStats)
- **一期状态**：🔵 保留 ⚠️ `client_id（客户ID）`/`lead_id（线索ID）`/`case_id（案件ID）` 裸字符串列，归因靠手机号兜底

#### MarketingMaterial（表名：marketing_materials（营销素材））`marketing`

- **功能**：早期素材表，现读写已统一到 `AdMaterial（广告素材）`。
- **前置条件**：无。
- **后置条件**：仅 seeds 兼容写入。
- **关键代码**：`marketing.service.ts:48`(注释「旧表 entity 仅供 seeds」)
- **一期状态**：🟡 软砍（旧表，双表并存风险）

#### ContentTemplate（表名：content_templates（内容模板））`marketing`

- **功能**：按案由+内容类型沉淀营销文案模板（短视频脚本/朋友圈文案/直播话术/图文）。
- **前置条件**：须 `case_type（案件类型）`+`content_type` 匹配模板（兜底 other）。
- **后置条件**：无跨模块联动，仅返回文本。
- **关键代码**：`content-generator.service.ts:458`(seedPresetTemplates) / `:504`(generateContent 模板变量替换)
- **一期状态**：🟠 演示壳（模板+变量替换，无真实 AI）

#### SocialAccount（表名：social_accounts（社交账号））`marketing`

- **功能**：抖音/快手/视频号/公众号等多平台账号矩阵，管理授权状态与统计。
- **前置条件**：须 `platform（平台）`/`account_id（账号ID）`。
- **后置条件**：授权写 `auth_status`/`auth_token`；无自动同步。
- **关键代码**：`social-account.service.ts:19`(create（创建）)
- **一期状态**：🟡 软砍（需平台 OAuth，一期未接入真实平台）

#### MarketingSocialPost（表名：marketing_social_posts（营销社交帖子））`marketing`

- **功能**：多账号内容排期、定时发布、互动数据统计。
- **前置条件**：须存在 `account_id（账号ID）`。
- **后置条件**：排期到点改 `status（状态）=PUBLISHED`，无真实平台发布调用。
- **关键代码**：`social-post.service.ts:160`(publish) / `:219`(autoPublishDue)
- **一期状态**：🟡 软砍

#### DigitalHumanLive（表名：digital_human_lives（数字人直播））`marketing`

- **功能**：数字人直播开播/结束/统计管理。
- **前置条件**：直播中(LIVE)禁止编辑/删除。
- **后置条件**：无跨模块联动。
- **关键代码**：`digital-human-live.service.ts:64`(startLive)
- **一期状态**：🟠 演示壳（无真实数字人流对接）

#### AdPlatformToken（表名：ad_platform_tokens（广告平台令牌））`ad-platforms`

- **功能**：存储巨量引擎/百度/腾讯/快手等 OAuth 授权后的 access/refresh token（令牌） 及过期时间。
- **前置条件**：须 `organization_id（组织ID）`+`platform（平台）`。
- **后置条件**：无业务联动，仅供给 DataSync 与各平台 client。
- **关键代码**：`token-manager.service.ts:23`(saveToken) / `:62`(getValidToken)
- **一期状态**：🟡 软砍（需外部平台凭证，一期不接入真实平台）

#### AdPlatformSyncLog（表名：ad_platform_sync_logs（广告平台同步日志））`ad-platforms`

- **功能**：记录每次定时拉取（余额/计划/报表）的执行结果。
- **前置条件**：基于 `token_status=active（是否启用）` 的 org+platform（平台） 组合。
- **后置条件**：余额同步更新 `AdAccount.balance`；结果落 `AdPlatformSyncLog（广告平台同步日志）`。
- **关联功能**：import（导入） `marketing/AdAccount`；各平台 client 真实 axios 调外部 API。
- **关键代码**：`data-sync.service.ts:81`(@Cron syncAllAccountBalances) / `:352`(writeSyncLog)
- **一期状态**：🟡 软砍

#### AdPlatforms Webhook（无独立实体）`ad-platforms`

- **功能**：封装各广告平台 OAuth、拉余额/计划/报表、回传转化；webhook（回调钩子） 接收平台线索/转化回调并归一化入库。
- **前置条件**：webhook（回调钩子） 须通过 HMAC-SHA256 验签；须能解析归属组织。
- **后置条件**：线索回调创建 `leads（线索）`、转化回调创建 `conversion_events（转化事件）`。
- **关联功能**：webhook（回调钩子） 注入 `lead/LeadService`(forwardRef)、`marketing/AdAccount`、`marketing/ConversionEvent`。
- **关键代码**：`webhook.controller.ts:64`(lead) / `:105`(conversion（转化）)
- **一期状态**：🟡 软砍（代码真实，运行依赖外部平台凭证与回调签名）

#### ChannelTracking（表名：scrm_channel_trackings（渠道追踪））`scrm`

- **功能**：按渠道统计扫码/加微/邀约/签约量，评估渠道转化效果。
- **前置条件**：须 `channel_name（渠道名称）`。
- **后置条件**：纯计数，无跨模块写入（计数由外部调用触发，非自动）。
- **关键代码**：`channel-tracking.service.ts:52`(recordScan 等)
- **一期状态**：🔵 保留

#### ChatArchive（表名：scrm_chat_archives（聊天存档））`scrm`

- **功能**：企微侧聊天记录存档，可检索并同步至合规质检。
- **前置条件**：须 `client_id（客户ID）`+`employee_id`。
- **后置条件**：同步质检后写 `compliance_synced`/`compliance_result（合规结果）`。
- **关联功能**：import（导入） `compliance/ComplianceService`。
- **关键代码**：`chat-archive.service.ts:19`(create（创建）) / `:111`(syncToCompliance)
- **一期状态**：🔵 保留

#### ClientTag（表名：scrm_client_tags（客户标签））/ 关系（表名：scrm_client_tag_relations）`scrm`

- **功能**：客户标签体系（来源/案由/意向/阶段），支持手动与按规则自动打标。
- **前置条件**：须 `tag_id`+`client_id（客户ID）`。
- **后置条件**：删标签级联删关系；自动打标供 ReachTask（触达任务） 筛选目标。
- **关联功能**：被 `scrm/SidebarService` 调用。
- **关键代码**：`client-tag.service.ts:18`(createTag) / `:155`(autoTagClient)
- **一期状态**：🔵 保留

#### LiveCode（表名：scrm_live_codes（活码））`scrm`

- **功能**：企微/个微/群活码，配置分流规则（轮询/负载/地域/案由）。
- **前置条件**：须 `code_type`+`name（名称）`。
- **后置条件**：`dispatch` 返回模拟 `target_user`，无真实企微跳转。
- **关键代码**：`live-code.service.ts:13`(create（创建）) / `:80`(dispatch)
- **一期状态**：🟠 演示壳（分流为模拟）

#### ReachTask（表名：scrm_reach_tasks（触达任务））`scrm`

- **功能**：1v1/朋友圈/社群 SOP 触达任务，按标签筛选目标客户。
- **前置条件**：须 `target_tags`。
- **后置条件**：`send` 计算 `target_count`/`sent_count` 并置 `sent`，未真实下发。
- **关键代码**：`reach-task.service.ts:16`(create（创建）) / `:86`(send)
- **一期状态**：🟠 演示壳（send 为模拟发送）

#### ScriptLibrary（表名：scrm_script_libraries（话术库））`scrm`

- **功能**：谈案/销售话术分类库（开场白/异议处理/促单等）。
- **前置条件**：须 `title（标题）`+`content（内容）`。
- **后置条件**：无真实发送。
- **关键代码**：`script-library.service.ts:13`(create（创建）) / `:57`(sendScript 模拟)
- **一期状态**：🟠 演示壳

#### SidebarService（无实体）`scrm`

- **功能**：企微侧边栏客户全景档案聚合（标签+跟进+来源+案件）与概览。
- **前置条件**：须 `client_id（客户ID）` 或 `phone（电话）`。
- **后置条件**：写 `FollowUp（跟进）`。
- **关联功能**：import（导入） `lead/Lead`、`lead/FollowUp`、`case/Case`、`user/User`、`scrm/ClientTagService`。
- **关键代码**：`sidebar.service.ts:29`(getClientProfile)
- **一期状态**：🔵 保留

#### SocialPost（社交帖子） / SocialComment（社交评论） / SocialLike（表名：social_posts（社交帖子） / social_comments（社交帖子） / social_likes（社交帖子））`social`

- **功能**：组织内部员工社区：发动态、评论（支持回复）、点赞，统计阅读/点赞/评论数，可关联案件。
- **前置条件**：动态需 `user_id（用户ID）`+`organization_id（组织ID）`；评论/点赞需 `post_id（帖子ID）`。
- **后置条件**：评论/点赞同步维护 `comment_count`/`like_count（点赞数）`；删除动态级联清理。
- **关联功能**：仅 import（导入） `user/User`，无跨模块业务调用（内部闭环）。
- **关键代码**：`social.service.ts:26`(createPost) / `:218`(likePost，`@Unique` 幂等)
- **一期状态**：🔵 保留（纯数据载体）

---

### 5.3 案件 / 合同 / 办案 / 合规 / 签署

#### Case（表名：cases（案件））`case`

- **功能**：案件核心载体，承载建案、分派、办案、结案/归档全生命周期，并聚合当事人、团队、费用、文档、预警。建案时自动生成编号、风险评级、利冲检索、类案匹配、SOP 生成。
- **前置条件**：必须 `organization_id（组织ID）`；利冲/编号规则可选（无则回退 `AJ-`/随机）。
- **后置条件**：落 `cases（案件）` 行；联动 `conflict_checks（利益冲突审查）`、`contracts（合同）`(contract_id（合同ID） 双向)、`receivables（应收）`、`commissions`、`client_ratings`、短信、审计。
- **关联功能**：注入 Contract/Receivable/CommissionService/ConflictCheckService/ComplianceService/ClientService(fwdRef)/SimilarCaseService/AuditService/LegalDocumentService/SmsService/NumberRuleService；被 fadada、seal（用印）、contract、property-preservation 反向引用。
- **关键代码**：`case.service.ts:172`(create（创建） 建案+编号+利冲+类案+SOP) / `:737`(closeCase 结案触发分润/评价) / `:955`(archiveCase 归档) / `:501`(assignLawyer)
- **一期状态**：🔵 保留 ⚠️ `fee_collected/invoiced_amount/settled_amount` 被动字段，主流程不主动维护；`closeCase`/`archiveCase` 应收合计→合同 is_settled（交回人ID） 逻辑重复易漂移

#### ConflictCheck（表名：conflict_checks（利益冲突审查））`case`

- **功能**：记录当事人/对方当事人的利益冲突检索结果，支持普通检索与「深度检索」（含模拟工商数据）。命中冲突时挂起案件待合规复核，是合规红线。
- **前置条件**：仅需 `organization_id（组织ID）` + 当事人/对方姓名；`case_id（案件ID）` 可空（检索时可能尚无案件）。
- **后置条件**：落 `conflict_checks（利益冲突审查）`；命中冲突回写 `cases.approval_status='conflict_hold'`；审批通过/驳回发通知。
- **关联功能**：被 CaseService.create、FadadaService.generateCaseFromContract 调用；注入 NotificationService。
- **关键代码**：`conflict-check.service.ts:26`(check) / `:151`(deepCheck 含 Math.random 伪造) / `:257`(approve/reject)
- **一期状态**：🔵 保留 ⚠️ `deepCheck` 工商数据伪造仅演示；reject（驳回） 联动案件更新包空 catch 静默失败

#### Contract（表名：contracts（合同））`contract`

- **功能**：合同管理覆盖起草→审查→发签待签→已签→履行→完成/解约/作废，打通用印、审批、交回、更正。新流程「线索→发合同→签约回调自动生成案件」由 fadada 驱动。
- **前置条件**：发签须有 `lead_id（线索ID）`；审批通过需 `lead_lawyer_id（主办律师ID）` 或 `approverId`；合同号优先走 number-rule。
- **后置条件**：落 `contracts（合同）`+`contract_stages`；审批通过→自动 `seal_applications（用印）`；签约完成→自动 `cases（案件）`+`receivables（应收）`+回写 `contract.case_id`/`lead.conversion_status`；用印盖章→合同 `stage（阶段）='signed（已签）'`。
- **关联功能**：注入 SealService、Case（案件）、AuditService、LegalDocumentService、NumberRuleService；被 fadada、case、seal（用印） 调用。
- **关键代码**：`contract.service.ts:111`(create（创建）) / `:634`(approve（审批） 自动建用印申请) / `:307`(findAll **同模板隐藏逻辑**：按案件查时隐藏「签发时间早于最近已签合同」的同模板 signing（待签） 合同)
- **一期状态**：🔵 保留（发签/签约走 fadada，支持 mock/prod/uat）
- ⚠️ 同模板隐藏仅为「列表查询展示」过滤，不删数据；`case_failed` 是 stage（阶段） 隐式扩展值不在枚举内；用印状态推导表达式有恒真分支

#### Contract（发签/签约回调，`fadada` 侧）`fadada`

- **关键方法**：`launchSignFromLead` 发合同(建 `stage（阶段）='signing（待签）'` 合同+签约合规记录+发起法大大签署任务，`fadada.service.ts:1308`)；`handleCallback` 签约完成回调(`sign-task-finished`→`generateCaseFromContract`，`:1627`)；`generateCaseFromContract` 签约即建案+建应收+利冲(`:1458`，案件与应收同事务 `:1559-1573`)；建案失败标 `case_failed` 不再静默(`:1709`)。
- **前置条件**：发签须有 `lead_id（线索ID）`（线索）。
- **后置条件**：签约完成自动 `cases（案件）`+`receivables（应收）`+回写 `contract.case_id`/`lead.conversion_status`；触发 C 端收案短信。
- **关联功能**：注入 ConflictCheckService(case)、Receivable/CommissionService(finance)、SmsService；被 sign-template.controller、client.service 调用。
- **一期状态**：🔵 保留 ⚠️ `case_id（案件ID）` 新流程发合同时存空串；分润调用已删(`:1598` 注释死代码，签约时案件未结案必 skipped)

#### Seal（用印） / SealApplication（用印申请） / SealRecord（表名：seal（用印） / seal_applications（用印） / seal_records（用印））`seal（用印）`

- **功能**：印章库 + 用印申请 + 盖章记录三段。合同审批通过后自动派生用印申请；审批/盖章/作废均回写合同用印状态，盖章完成可推进合同至 `signed（已签）`。
- **前置条件**：用印申请须 `seal_id`（审批通过时用组织下任一 active（是否启用） 印章，缺省伪 UUID）。
- **后置条件**：落 `seal_applications（用印）`+`seal_records（用印）`；联动 `contracts.seal_usage_status/electronic_seal_status/paper_seal_status` 及 `contracts.stage`。
- **关联功能**：注入 Contract（合同）、AuditService、User（用户）；被 ContractService 调用。
- **关键代码**：`seal.service.ts:103`(createApplication) / `:197`(approveApplication) / `:270`(useApplication 盖章→`stage（阶段）='signed（已签）'`)
- **一期状态**：🔵 保留 ⚠️ `useApplication` 会把 drafting/reviewing 直接置 signed（已签） 跳过 signed/performing；findRecords 内存二次过滤

#### PropertyPreservation（表名：property_preservation（财产保全））`property-preservation`

- **功能**：财产保全管理（诉前/诉讼/仲裁/执行），覆盖草稿→审批→实施→解除/过期，每日定时扫描到期保全、生成案件预警。
- **前置条件**：须 `case_id（案件ID）`（可空但解除/过期依赖它回写）；须 `applicant/respondent` 非空。
- **后置条件**：落 `property_preservation（财产保全）`；解除/过期静默回写关联 `cases.risk_notes`；过期触发 `case_warnings`。
- **关联功能**：注入 Case（案件）、CaseWarning。
- **关键代码**：`property-preservation.service.ts:73`(create/submit/approve) / `:248`(@Cron markExpired 每日9点扫描)
- **一期状态**：🔵 保留 ⚠️ 回写案件 `risk_notes（风险说明）` 的 catch 全空，失败不可见

#### Document（案件文档，表名：documents）`case`

- **功能**：案件文档随案上传（本地 `uploads/documents/{caseId}`、可触发短信）。
- **前置条件**：案件文档需 `case_id（案件ID）`。
- **后置条件**：上传「成交合同」触发 `filing` 短信、「律师函」触发 `lawyer_letter` 短信。
- **关联功能**：CaseService 直接使用 `Document（文档）` 实体。
- **关键代码**：`case.service.ts:522`(uploadDocumentFile)
- **一期状态**：🔵 保留 ⚠️ 与独立文档库 `document_items（文档条目）` 并存两套表

#### DocumentItem（表名：document_items（文档条目））`document`

- **功能**：独立文档库，支持版本管理与回滚。
- **前置条件**：需 `uploader_id`+`organization_id（组织ID）`。
- **后置条件**：版本写入 `document_versions`。
- **关键代码**：`document.service.ts:18`(create/findList) / `:createVersion` / `:rollbackToVersion`
- **一期状态**：🔵 保留（与案件文档并存，未统一抽象）

#### ArchiveVolume（表名：archive_volumes（归档卷宗））`archive-volume`

- **功能**：卷宗归档与借阅管理（电子/纸质），记录卷宗编号、借阅人、借还日期、卷宗文件。
- **前置条件**：须 `case_id（案件ID）` + `volume_no` + `name（名称）` + `organization_id（组织ID）`。
- **后置条件**：落 `archive_volumes（归档卷宗）`；无自动联动。
- **关联功能**：仅自包含（未注入其它 Service）。
- **关键代码**：`archive-volume.service.ts:14`(create（创建）) / `:64`(borrow)
- **一期状态**：🔵 保留 ⚠️ **Case.archiveCase 仅写 `cases.archive_no`，并不创建 `archive_volumes（归档卷宗）`**——卷宗与案件归档两套互不相通，存在「案已归档但无卷宗」风险

#### NumberRule（编号规则） / NumberSequence（编号序列） / NumberDepartment（表名：number_rules（编号规则） / number_sequences（编号规则） / number_departments（编号规则））`number-rule`

- **功能**：统一编号引擎，按组织+编号类型(case/contract/legal_document/archive)+业务类型+部门+年份生成编号，支持模板占位符与流水号补零。
- **前置条件**：调用方须传 `orgId`+`numberType`+`bizType`；无启用规则时返回 `null`，各调用方回退 `AJ-`/`HT-`/随机。
- **后置条件**：落/递 `number_sequences（编号规则）`；产出编号被 cases/contracts/legal_documents/archive 使用。
- **关联功能**：被 CaseService、ContractService、FadadaService 广泛注入。
- **关键代码**：`number-rule.service.ts:114`(generateNumber) / `:188`(nextSeq 事务内递增)
- **一期状态**：🔵 保留（被广泛依赖；无规则时各调用方回退随机编号）

#### SignTemplate（表名：sign_templates（签约模板））`fadada`

- **功能**：法大大「签署任务模板」的本地镜像，记录 `sign_template_id`、名称、归属组织、启用状态、互动视频签播报配置。
- **前置条件**：模板需在法大大后台先创建，再将 `sign_template_id` 录入；`enabled（是否启用）=true` 才可被选中。
- **后置条件**：被 `launchSignFromTemplate/launchSignFromLead` 读取并传入 `createSignTaskFromTemplate`；跨模块写 `Contract.template_id`/`signing_compliance.contract_template_id`。
- **关键代码**：`sign-template.entity.ts:8`；`SignTemplateService.list:18` / `upsert:33`
- **一期状态**：🔵 保留

#### SignTemplateField（表名：sign_template_fields（签约模板字段））`fadada`

- **功能**：法大大模板内填写控件的本地配置（控件名/类型/参与方/填写方式/自动带出键/固定值）。
- **前置条件**：`SignTemplate（签约模板）` 存在；`sync-fields` 调 `getTemplateFillFields` 拉取法大大控件。
- **后置条件**：发起签约时 `fillValues` 据此组装。
- **关键代码**：`sign-template-field.entity.ts:12`；`fillValuesWithTolerance:920`(金额控件 API 无法填充，显式跳过)
- **一期状态**：🔵 保留 ⚠️ 本地配置与远端模板不同步时是静默风险

#### SigningCompliance（表名：signing_compliance（签约合规））`fadada`+`compliance（合规）`

- **功能**：承接「法大大电子签 + 风险告知 + 合同合规」三类动作。B端发起签约时落 pending（待处理） 记录，C端完成实名/签署后由回调改状态，新流程还在签约完成时联动生成案件。fadada 与 compliance（合规） 两模块共用。
- **前置条件**：依赖 `SignTemplate（签约模板）`(模板)、`Contract（合同）`/`Lead（线索）`/`ClientProfile（客户档案）`、`CaseStatusConfig`、`NumberRuleService`、`ConflictCheckService`、`SmsService`。
- **后置条件**：状态流转 pending（待处理）→reviewing→signed（已签）；新流程回写 `contract.stage=signed（已签）`+`case_id（案件ID）`、回写 `lead` 转化状态、联动 `Case（案件）`/`Receivable（应收）`；签约完成触发 C 端收案短信。
- **关联功能**：import（导入） `SigningCompliance（签约合规）`(compliance（合规）)、`Contract/Case/Lead/ClientProfile`、`ConflictCheckService`(case)、`Receivable/CommissionService`(finance)、`SmsService`；被 client.service 调用。
- **关键代码**：`fadada.service.ts:1186`(launchSignFromTemplate) / `:960`(completeClientPrefillAndSign)；compliance（合规） 侧 `createSigningCompliance:411`
- **一期状态**：🔵 保留（全链路枢纽）⚠️ `risk_disclosure_id` 裸外键指向未注册的 RiskDisclosure（风险告知） 实体

#### CorpAuth（表名：corp_auths（企业认证））`fadada`

- **功能**：平台型应用为接入的其他企业生成法大大授权链接，授权完成后回填 `open_corp_id` 与授权/认证状态。
- **前置条件**：法大大企业授权能力开通；`clientCorpId` 唯一。
- **后置条件**：回填 `open_corp_id/binding_status/ident_status`。
- **关键代码**：`corp-auth.service.ts:68`(create（创建）) / `:143`(queryStatus)
- **一期状态**：🔵 保留（自建应用可软砍）

#### ComplianceRecord（表名：compliance_records（合规记录））`compliance（合规）`

- **功能**：通用合规内容抽检（营销/销售话术等）关键词命中记录，存结果/违规类型/建议。
- **前置条件**：调用方提供 content/type/orgId/operatorId。
- **后置条件**：落库一条记录，供导出归档。
- **关键代码**：`compliance-record.service.ts:6`；`checkCompliance:129`(命中 absolute/promise/exaggerate 即 REJECT)
- **一期状态**：🔵 保留（纯关键词规则，无 NLP）

#### MarketingContent（表名：marketing_contents（营销内容））`compliance（合规）`

- **功能**：营销素材提交与合规审核（草稿/待审/通过/驳回），带违规问题与建议。
- **前置条件**：operator_id（操作人ID） 关联 users（用户）。
- **后置条件**：命中关键词置 pending_review，否则 approved。
- **关键代码**：`marketing-content.service.ts:275`(createMarketingContent)
- **一期状态**：🔵 保留

#### SalesCompliance（表名：sales_compliance（销售合规））`compliance（合规）`

- **功能**：销售/谈案话术合规留痕，含风险告知接受状态与人工复核。
- **前置条件**：lead_id（线索ID） 关联线索。
- **后置条件**：存 check_result（检查结果）、risk_disclosure_accepted。
- **关键代码**：`sales-compliance.service.ts:370`(createSalesCompliance)
- **一期状态**：🔵 保留

#### CaseSOP（表名：case_sop（案件SOP））`compliance（合规）`

- **功能**：原案件 SOP 步骤表。现已合并到 `CaseTask`，`createCaseSOP` 实际写入 `CaseTask` 表，本表不再有写入路径。
- **前置条件**：case_id/case_type。
- **后置条件**：生成 CaseTask 节点，校验强制节点。
- **关键代码**：`compliance.service.ts:461`(createCaseSOP 生成 CaseTask)
- **一期状态**：🟡 软砍（`case_sop（案件SOP）` 表成遗留空表，一期可删或仅保留只读）

#### TalkQualityCheck（表名：talk_quality_checks（话术质检））`compliance（合规）`

- **功能**：谈案通话/聊天 AI 质检，命中虚假承诺/夸大等违规并生成处理记录。
- **前置条件**：invite_task_id 关联 lead 模块 `InviteTask（邀约任务）`。
- **后置条件**：落质检记录，可触发通知。
- **关联功能**：ManyToOne `InviteTask（邀约任务）`(lead)、`User（用户）`；被谈案/质检模块调用。
- **关键代码**：`talk-quality-check.service.ts:40`；`runTalkQualityCheck:604`
- **一期状态**：🔵 保留（同样依赖关键词，无真正模型）

#### ComplianceRule（表名：compliance_rules（合规规则））`compliance（合规）`

- **功能**：可配置合规检查规则（阶段/类型/关键词或正则），供巡检触发。
- **前置条件**：规则 enabled（是否启用）。
- **后置条件**：巡检更新 `compliance_check_results（合规检查结果）`。
- **关键代码**：`compliance-rule.service.ts:18`；`triggerInspection:976`
- **一期状态**：🔵 保留

#### ComplianceCheckResult（表名：compliance_check_results（合规检查结果））`compliance（合规）`

- **功能**：聚合各域（营销/销售/签约/办案/财务）合规检查结果的统一宽表，含 domain/check_type/target/risk_level。
- **前置条件**：各域检查写入。
- **后置条件**：统一留痕，供看板。
- **关键代码**：`compliance-check-result.entity.ts:68`；`getCheckResults:929`
- **一期状态**：🔵 保留（统一结果表）

#### FinanceComplianceCheck（表名：finance_compliance_checks（财务合规检查））`compliance（合规）`

- **功能**：应收/发票/分润三类财务合规校验占位。
- **前置条件**：传入 target_id（目标ID）。
- **后置条件**：仅生成 `check_result（检查结果）=PASS` 记录，无真实校验逻辑。
- **关键代码**：`finance-compliance-check.service.ts:1061`(checkReceivable 等全写死 PASS)
- **一期状态**：🟠 演示壳（一期若上线需补真实规则，否则误导）

#### CaseComplianceCheck（表名：case_compliance_checks（案件合规检查））`compliance（合规）`

- **功能**：案件超期/文书/证据/SOP节点等交付合规检查台账。
- **前置条件**：CaseTask 存在。
- **后置条件**：生成 HIGH 级违规检查记录。
- **关键代码**：`case-compliance-check.service.ts:43`；`triggerCaseInspection:1559`(扫描 CaseTask 超期)
- **一期状态**：🔵 保留（事后扫描，非实时阻断）

#### CasePersonnelChange（表名：case_personnel_changes（案件人员变更））`compliance（合规）`

- **功能**：案件主办/协办/转委托变更审批流。
- **前置条件**：case_id（案件ID）。
- **后置条件**：待审批变更会阻断结案归档。
- **关键代码**：`case-personnel-change.entity.ts:27`；`approvePersonnelChange:1647`
- **一期状态**：🔵 保留 ⚠️ 审批通过仅改状态，未联动改写 `Case.assignee_lawyer_id`

#### CaseArchive（表名：case_archives（案件归档））`compliance（合规）`

- **功能**：案件结案归档校验（材料清单+SOP节点闭环+人员变更），产出电子卷宗。
- **前置条件**：CaseSOP（案件SOP） 强制节点全过、无待审人员变更。
- **后置条件**：生成 archived（已归档） 记录与 archive_path。
- **关键代码**：`case-archive.entity.ts:48`；`checkCaseArchive:1692` / `executeArchive:1757`
- **一期状态**：🔵 保留 ⚠️ 材料清单硬编码 `uploaded:true`，未校验真实上传

#### ComplaintTicket（表名：complaint_tickets（投诉工单））`compliance（合规）`

- **功能**：客户投诉统一工单（来源/类型/严重级/处理记录 JSON/归档/升级），闭环管理。取代旧 `complaints（投诉）` 表。
- **前置条件**：case_id/client_id 可选。
- **后置条件**：状态流转+process_records 追加，关闭即归档。
- **关联功能**：ManyToOne `Case（案件）`/`User（用户）`/`Organization（组织）`；被客诉/舆情模块调用（投诉率看板数据源）。
- **关键代码**：`complaint-ticket.entity.ts:60`；`createComplaintTicketFull:1198`
- **一期状态**：🔵 保留（投诉率看板读此表）

#### Complaint（表名：complaints（投诉））`compliance（合规）`

- **功能**：旧投诉表。当前 `createComplaint` 实际改写为写 `ComplaintTicket（投诉工单）`，旧表不再写入。
- **前置条件**：无实质写入。
- **后置条件**：仅映射旧状态到 ticket 状态。
- **关键代码**：`compliance.service.ts:194`(createComplaint 转写 ticket)
- **一期状态**：🟡 软砍（旧表 `complaints（投诉）` 与 `complaint_tickets（投诉工单）` 并存，属合并遗留双写）

#### RiskDisclosure（表名：risk_disclosures（风险告知））`compliance（合规）`

- **功能**：风险告知签署记录。
- **前置条件**：无。
- **后置条件**：无。
- **关联功能**：被 `SigningCompliance.risk_disclosure` ManyToOne 引用，但本实体**未注册进 compliance.module**。
- **关键代码**：`risk-disclosure.entity.ts:3`
- **一期状态**：⚫ 死代码（`signing_compliance.risk_disclosure_id` 实际悬空，真实告知数据无处落库）

#### ContractTemplate（表名：contract_templates（合同模板））`compliance（合规）`

- **功能**：本地合同模板。
- **前置条件**：无。
- **后置条件**：无。
- **关键代码**：`contract-template.entity.ts:3`（全局 grep 无引用）
- **一期状态**：⚫ 死代码（未注册，无 service/controller 引用）

#### LawyerQualification（表名：lawyer_qualifications（律师资质））`compliance（合规）`

- **功能**：律师执业证/资格状态。
- **前置条件**：无。
- **后置条件**：无。
- **关键代码**：`lawyer-qualification.entity.ts:16`（无 service 引用）
- **一期状态**：⚫ 死代码（签约合规的「律师资格已核验」只是写死 `true`）

#### DueDiligence（表名：due_diligences（尽职调查））`due-diligence`

- **功能**：按企业+查询类型生成尽调报告（股东/法人/财务/风险结构化字段 + 文本报告）。
- **前置条件**：company_name/query_type/orgId。
- **后置条件**：落一条 completed（已完成） 记录（状态默认 completed（已完成））。
- **关键代码**：`due-diligence.service.ts:14`(create（创建）) / `:130`(generateMockReport 硬编码模拟)
- **一期状态**：🟠 演示壳（报告为「尽调宝模拟数据」，无真实三方数据源）

#### Bid（表名：bids（投标））`bid`

- **功能**：律所投标项目台账（项目/招标方/金额/截止/状态：准备中→已投→中标/未中）。
- **前置条件**：project_name/tenderer/bid_amount/deadline/orgId。
- **后置条件**：状态变更，可关联业绩库（人工）。
- **关键代码**：`bid.service.ts:19`(createBid) / `:71`(win)
- **一期状态**：🔵 保留（与 bid_records（投标记录） 无外键联动）

#### BidRecord（表名：bid_records（投标记录））`bid`

- **功能**：律所历史业绩档案（项目/客户/金额/分类/审核状态/附件）。
- **前置条件**：project_name/client/amount/start_date/category/orgId。
- **后置条件**：待审核记录，审核后 approved。
- **关键代码**：`bid-record.service.ts:23`(createRecord)
- **一期状态**：🔵 保留

#### MeetingRoom（模块为空）`meeting-room`

- **功能**：需求文档提及，但 `backend/src/meeting-room/` 为空目录，无任何实现。
- **一期状态**：⚫ 整体砍掉/未实现（一期直接剔除或留作后续占位）

---

### 5.4 客户 / 财务 / HR

#### ClientProfile（表名：client_profiles（客户档案））`client`

- **功能**：B/C 端共用的客户主数据（个人/企业、证件号、来源、价值等级），是签约实名与短信触达的载体。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：回写 `id_card_no（企业名称）` 供法大大实名匹配。
- **关联功能**：被 ClientService 依赖；联动 compliance(SigningCompliance（签约合规）)、fadada、sms（短信）。
- **关键代码**：`client.service.ts:470`(onlineSign 回写身份证号) / `:519`(getSignVerifyUrl)
- **一期状态**：🔵 保留 ⚠️ C 端客户档案与 `user` 表两套身份，映射靠 `phone（电话）` 隐式关联

#### ClientConsultation（表名：client_consultations（客户咨询））`client`

- **功能**：记录客户 AI 法律咨询与回答，命中关键词（投诉/转人工/律师）自动转人工工单并转线索。
- **前置条件**：client_id（客户ID）、question。
- **后置条件**：建 Complaint(type（类型）=consultation_transfer（自动转人工生成工单）)+ Lead(CRM)+ 咨询记录。
- **关联功能**：注入 ComplianceService、LeadService(forwardRef)、User（用户）。
- **关键代码**：`client.service.ts:335`(aiConsultEnhanced) / `:393`(createLeadFromConsultation)
- **一期状态**：🔵 保留 ⚠️ `aiConsult` 返回写死模板回答，一期无真实模型；转线索失败静默

#### ServiceRating（表名：service_ratings（服务评价））`client`

- **功能**：客户结案后评分（1-5），低分自动生成客诉预警，好评可沉淀为营销素材。
- **前置条件**：case_id（案件ID）、client_id（客户ID）、rating（评分）。
- **后置条件**：低分→ComplaintTicket（投诉工单）；好评≥4→AdMaterial（广告素材）；结案→推送。
- **关联功能**：注入 ComplaintTicket(compliance（合规）)、AdMaterial(marketing)。
- **关键代码**：`client.service.ts:918`(createServiceRating 评分≤2 触发预警) / `:991`(convertRatingToMaterial)
- **一期状态**：🔵 保留

#### ClientArchive（表名：client_archives（客户归档））`client`

- **功能**：客户自行归档案件文书/证据/合同/发票/函件，支持 C 端上传与管理端检索。
- **前置条件**：client_id（客户ID）。
- **后置条件**：仅归档存储，无跨模块联动。
- **关键代码**：`client.service.ts:1088`(uploadArchive) / `:1140`(getClientArchives)
- **一期状态**：🔵 保留（归档与 Case（案件） 的 documents 表两套文件体系）

#### CasePushNotification（表名：case_push_notifications（案件推送通知））`client`

- **功能**：案件节点（立案/开庭/判决/结案）变更时向客户推送标准化进度，屏蔽敏感信息。
- **前置条件**：case_id（client_id（客户ID） 可空）。
- **后置条件**：仅插入推送记录，实际发送渠道在别处。
- **关联功能**：依赖 Case（案件）；被 Case（案件） 节点变更调用。
- **关键代码**：`client.service.ts:291`(triggerPushOnNodeChange)
- **一期状态**：🔵 保留 ⚠️ `client_id（客户ID）` 允许空，推送可能漏发

> C 端投诉提交/签约预填（跨 compliance（合规）+fadada）：投诉 `createComplaint` 写 compliance（合规） 的 `Complaint（投诉）` 并同步建 `ComplaintTicket（投诉工单）`(source（来源）=CLIENT_PORTAL)，同步失败仅 console.error 不阻断；签约 `onlineSign→getSignVerifyUrl→createSignFlow→submitSignPrefillAndSign` 操作 SigningCompliance（签约合规） 并调 FadadaService。

#### BusinessFund（表名：business_funds（业务款））`finance`

- **功能**：所有收入（律师费/收款）与支出（案件成本/代扣）最终都落到这张表，是财务收支单一事实来源。`type（类型）` 区分 income/expense，`category（类别）` 区分费种。
- **前置条件**：依赖 Case（案件）、Receivable（应收）、CommissionService。
- **后置条件**：联动改动 `case.fee_collected`、`case.settled_amount`；收满联动 CommissionRecord（佣金记录）；支出侧由 CaseCost/Withholding 同步写入。
- **关联功能**：被 CaseCostService、FinancialAccountingService、FinanceService、ReconciliationService 注入；写 Case（案件）。
- **关键代码**：`business-fund.service.ts:111`(accountFund 回写 settled_amount（已结算金额）) / `:144`(allocateFund 分账触发分润)
- **一期状态**：🔵 保留（核心账本）⚠️ `redFlushFee` 生成负数冲销，`reversal_of_id` 软关联无外键

#### Fee（表名：fees（费用））`finance`

- **功能**：历史上承载律师费记录，现业务侧 `createFee/findFees/voidFee/redFlushFee` 全部绕开 `fees（费用）` 表改读写 `business_funds（业务款）`。`fees（费用）` 表实际仅由 seeds 写入。
- **前置条件**：无。
- **后置条件**：仅 seeds 写入，不参与实时财务流。
- **关联功能**：被 `dashboard.service.ts` 按 `Fee.paid` 读取（近期回款/营收指标）。
- **关键代码**：`finance.service.ts:46-132`(同名方法已重定向到 BusinessFund（业务款）)
- **一期状态**：🟡 软砍/演示壳 ⚠️ 严重坑点：Dashboard（仪表盘） 仍按 `Fee.paid` 统计，但一期收入已写入 business_funds（业务款），指标可能恒为空；`refund.entity.ts:62` 仍 @ManyToOne Fee（费用），refund 实际关联对象已不存在

#### ProfitShare（表名：profit_shares（分润））`finance`

- **功能**：按角色（机构/律师/销售/市场/助理）百分比手动拆分某一笔费用的利润归属，用于单案利润分析。与 CommissionRecord（自动分润发放）是两套平行体系。
- **前置条件**：传入 caseId、orgId、feeAmount、rules。
- **后置条件**：仅供分析，不发放、不联动账户。
- **关键代码**：`finance.service.ts:243`(calculateProfitShare)
- **一期状态**：🔵 保留 ⚠️ 从不填 `user_id（用户ID）`，分润对象与具体人脱节；真正「发给谁」由 CommissionRecord（佣金记录） 承担

#### CommissionRule（表名：commission_rules（佣金规则））`finance`

- **功能**：定义各角色（投放/邀约/谈案/主办/协办/助理）的提成规则，支持固定值/比例/阶梯、按案由或通用、启停。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：被 `calculateCommission` 匹配使用。
- **关联功能**：被 CommissionService 使用；FinanceModule 导出 CommissionService 供 CaseModule 调用。
- **关键代码**：`commission.service.ts:28`(createRule/updateRule/toggleRule)
- **一期状态**：🔵 保留

#### CommissionRecord（表名：commission_records（佣金记录））`finance`

- **功能**：案件「结案且全款到账」时，按规则为各角色生成待发放提成记录，是财务实际打款依据。
- **前置条件**：Case（案件） 已结案 + Receivable（应收） 已完成。
- **后置条件**：`case.service.ts:766`(结案)、`:989`(归档)、`finance.service.ts:202`(收满)、`business-fund.service.ts:158`(分账) 触发生成；`markPaid` 发放置 paid（已付）。
- **关联功能**：注入 Case/User/Receivable；被 CaseModule、FinanceService、BusinessFundService 调用。
- **关键代码**：`commission.service.ts:125`(calculateCommission) / `:303`(checkAndTriggerCommission)
- **一期状态**：🔵 保留（真实发放引擎）⚠️ 收满时异常被静默吞掉(`finance.service.ts:203`)；`getCaseParticipants` 仅取主办律师，协办/邀约/谈案岗 TODO 未实现；时序敏感（先收满后结案才成功）

#### Receivable（表名：receivables（应收））`finance`

- **功能**：按案件登记合同额、已收、待收与分期计划，是收款、逾期、对账、分润的共同锚点。
- **前置条件**：case_id（与案件强绑定，非空）。
- **后置条件**：状态变更驱动 OverdueWarning（逾期预警）、CommissionRecord（佣金记录）、Case.fee_collected。
- **关联功能**：被 OverdueWarningService、ReconciliationService、CommissionService 注入。
- **关键代码**：由 `FinanceService.recordPayment`(`:172`)、`approveRefund`(`:341`) 维护
- **一期状态**：🔵 保留（资金流中枢）⚠️ 应收金额与 `cases.fee_amount`/`contracts.amount` 三处独立维护，无单一真相源

#### PaymentRecord（表名：payment_records（收款记录））`finance`

- **功能**：记录每次实际到账（客户/方式/流水号），是回款明细与对账基础。
- **前置条件**：必须存在 Receivable（应收）。
- **后置条件**：写 Receivable（应收） 汇总、回写 Case（案件）、可能触发分润。
- **关联功能**：被 ClientService 只读查询、ReconciliationService 汇总、PaymentReminderService 调用。
- **关键代码**：`finance.service.ts:143`(recordPayment 事务内回写应收+触发分润)
- **一期状态**：🔵 保留 ⚠️ **无 organization_id（组织ID） 字段**，只能经 Case（案件） 联表过滤；`client_id（客户ID）` 可为 `'pending（待处理）'`，C 端未绑定账号时统计失真

#### Refund（表名：refunds（退款））`finance`

- **功能**：客户退费全生命周期：pending（待处理）→approved→paid（已付），并回冲应收台账。
- **前置条件**：case_id（案件ID）、amount（金额）、reason（原因）。
- **后置条件**：改动 Receivable（应收） 已收/待收；status（状态） 流转。
- **关键代码**：`finance.service.ts:327`(approveRefund) / `:379`(payRefund)
- **一期状态**：🔵 保留 ⚠️ `approveRefund` 按 case_id（案件ID） 找唯一 Receivable（应收），多台账场景会误改；`fee（费用）` 字段指向已废弃 Fee（费用） 实体

#### Invoice（表名：invoices（发票））`finance`

- **功能**：开票、作废、冲红、退款、调账，并维护案件已开票额。
- **前置条件**：case_id（案件ID）、amount（金额）。
- **后置条件**：回写 `case.invoiced_amount`；作废/冲红计入 Reconciliation（对账） 的 mismatch。
- **关联功能**：被 ReconciliationService 注入；C 端 `downloadInvoice` 仅拼装数据不真开票。
- **关键代码**：`invoice.service.ts:45`(create（创建） 自动算税额) / `:27`(recalcCaseInvoicedAmount)
- **一期状态**：🔵 保留 ⚠️ 旧字段与新增强字段、`status（状态）` 与 INVOICE_EXT_STATUS 两套并存，语义歧义

#### CaseCost（表名：case_costs（案件成本））`finance`

- **功能**：登记办案各类成本（投放/保全/诉讼/差旅等），自动同步进统一账本 BusinessFund（支出）。
- **前置条件**：case_id（案件ID）、cost_type（成本类型）、amount（金额）。
- **后置条件**：写入 BusinessFund(expense)→进入收支汇总与单案利润分析。
- **关键代码**：`case-cost.service.ts:42`(create（创建） 事务内同步写 BusinessFund（业务款）) / `:98`(syncCostToFinance)
- **一期状态**：🔵 保留 ⚠️ 作废靠 `remarks（备注） LIKE '%case_cost:${id（编号）}%'` 匹配，remarks（备注） 被改则失效；同步失败 try/catch 静默

#### OverdueWarning（表名：overdue_warnings（逾期预警））`finance`

- **功能**：每日 1:00 扫描未结清应收，计算逾期金额/天数，标记 Receivable（应收）=OVERDUE 并推送通知。
- **前置条件**：Receivable（应收） 存在且 pending/partial。
- **后置条件**：改 Receivable.status、installment_plan（分期计划） 状态；建/更预警；发通知。
- **关联功能**：注入 NotificationService(user)。
- **关键代码**：`overdue-warning.service.ts:24`(@Cron checkOverdueReceivables) / `:90`(batchCreateOrUpdateWarnings)
- **一期状态**：🔵 保留 ⚠️ 通知 `receiver_id: ''` 空值，一期通知可能发空/丢失；整段 catch 静默

#### Reconciliation（表名：reconciliations（对账））`finance`

- **功能**：按周期汇总应收/已收/逾期/匹配率，并交叉校验支付记录与发票异常。
- **前置条件**：orgId、周期。
- **后置条件**：生成对账记录；mismatch 计数（支付额≠已收额、异常发票均追加）。
- **关联功能**：注入 InvoiceService、PaymentRecord（收款记录）、Receivable（应收）。
- **关键代码**：`reconciliation.service.ts:47`(runReconciliation) / `:156`(getReconciliationStats)
- **一期状态**：🔵 保留（只读计算）⚠️ 支付记录汇总失败、发票查询失败均静默，对账可能「假平」

#### WithholdingBatch（代扣批次） / WithholdingRecord（表名：withholding_batches（代扣批次） / withholding_records（代扣批次））`finance`

- **功能**：个税/工资/固定费用代扣的批次与明细管理；执行后写入 BusinessFund（业务款） 支出，形成资金闭环。
- **前置条件**：user_id/case_id、amount（金额）、type（类型）。
- **后置条件**：写入 BusinessFund(expense)→进统一账本；更新批次 success/fail 计数。
- **关键代码**：`financial-accounting.service.ts:95`(createBatch) / `:219`(executeWithholding)
- **一期状态**：🔵 保留 ⚠️ 与案件/员工真实关联关系弱（case_id/user_id 可空），可追溯性有限

#### HrLeave（表名：hr_leaves（HR请假））`hr`

- **功能**：员工请假申请与审批流（pending（待处理）→approved/rejected/cancelled）。
- **前置条件**：user_id（用户ID）、orgId。
- **后置条件**：仅状态流转，无跨模块改动。
- **关键代码**：`hr.service.ts:37`(createLeave) / `:100`(approveLeave)
- **一期状态**：🔵 保留（边缘）

#### Attendance（表名：attendances（考勤））`hr`

- **功能**：上下班打卡，自动判定迟到(>9:00)/早退(<18:00)与工时。
- **前置条件**：user_id（用户ID）、orgId、当日记录。
- **后置条件**：写考勤记录，无外部联动。
- **关键代码**：`hr.service.ts:188`(clockIn) / `:220`(clockOut)
- **一期状态**：🔵 保留（边缘）

#### MaterialRequisition（表名：material_requisitions（物料领用））`hr`

- **功能**：办公物品申购/领用审批（pending（待处理）→approved→fulfilled）。
- **前置条件**：user_id（用户ID）、orgId。
- **后置条件**：无跨模块。
- **关键代码**：`hr.service.ts:271`(createMaterial)
- **一期状态**：🔵 保留（边缘）

#### HrActivity（HR活动） / ActivityRegistration（表名：hr_activities（HR活动） / activity_registrations（HR活动））`hr`

- **功能**：内部活动创建与员工报名，维护报名人数计数。
- **前置条件**：activity_id、user_id（用户ID）。
- **后置条件**：更新活动报名计数。
- **关键代码**：`hr.service.ts:447`(registerActivity 同步 registered_count)
- **一期状态**：🔵 保留（边缘）⚠️ `registered_count` 应用层自增，并发可能超员

---

### 5.5 AI / 知识 / 协同 / 工作 / 聚合

#### ContractReview（表名：contract_reviews（合同审查））`ai`

- **功能**：合同审查记录。基于内置风险规则库（违约金/自动续约/管辖/免责等关键词命中）识别风险条款，计算风险等级并落库。
- **前置条件**：调用方传入 `contract_text（风险等级）`、`organization_id（组织ID）`。
- **后置条件**：写入 `contract_reviews（合同审查）`（status（状态） 恒 completed（已完成），risk_level（风险等级） 由命中数推导）。
- **关联功能**：仅注入 Repository，无跨模块调用。
- **关键代码**：`ai-review.service.ts:116`(reviewContract) / `:207`(getContractReviews)
- **一期状态**：🔵 保留（规则引擎，非真 AI）⚠️ 正则匹配误报率高，status（状态） 写死 completed（已完成）

#### LegalResearch（表名：legal_researches（法律检索））`ai`

- **功能**：法律研究任务记录。按主题关键词拼装「研究要点/参考依据」，实为固定话术。
- **前置条件**：topic（关键词JSON） 非空。
- **后置条件**：落库一条研究记录。
- **关键代码**：`ai-review.service.ts:263`(createResearchTask)
- **一期状态**：🔵 保留（模板生成，非真 AI）

#### AiService（无表，演示壳）`ai`

- **功能**：营销文案、视频脚本、法律文书、案件风险分析。全部硬编码模板字符串，无任何 Repository 注入。
- **关键代码**：`ai.service.ts:5`(generateMarketingCopy) / `:50`(generateLegalDocument 拼接) / `:162`(analyzeCaseRisk)
- **一期状态**：🟠 演示壳 ⚠️ `AiController` 的 `/chat`、`/similar-cases`、`/laws` 返回空 data 数组，前端需自备兜底

#### KnowledgeArticle（表名：knowledge_articles（知识文章））`knowledge`

- **功能**：律所知识文章 CRUD，含浏览量自增。
- **前置条件**：organization_id（组织ID）。
- **关键代码**：`knowledge.service.ts:44`(createArticle) / `:109`(incrementView)
- **一期状态**：🔵 保留

#### LawRegulation（表名：law_regulations（法律法规））`knowledge`

- **功能**：法律法规库 CRUD（标题/分类/生效日/全文）。
- **关键代码**：`knowledge.service.ts:121`
- **一期状态**：🔵 保留

#### CasePrecedent（表名：case_precedents（案例判例））`knowledge`

- **功能**：裁判文书库 CRUD（案号/法院/裁判类型/全文）。
- **关键代码**：`knowledge.service.ts:174`
- **一期状态**：🔵 保留

#### LawTool（无表，演示壳）`law-tool`

- **功能**：法律工具导航，返回 11 大类 100+ 外部网站链接（北大法宝、裁判文书网等），纯静态常量。
- **关键代码**：`law-tool.service.ts:206`(findList) / `LAW_TOOL_CATEGORIES:4`
- **一期状态**：🟠 演示壳（链接硬编码，无任何 DB 交互，本质「书签页」）

#### Task（表名：tasks（任务））`task`

- **功能**：任务管理，含状态机（pending（待处理）→processing（处理中）→completed/cancelled）、复核。
- **前置条件**：`assignee_id（承办人ID）`、`organization_id（组织ID）`。
- **后置条件**：完成给负责人加经验+通知创建人；进度≥100 自动完成。
- **关联功能**：注入 `UserService`(addExperience)、`NotificationService`；被 workbench、dashboard 读取。
- **关键代码**：`task.service.ts:28`(create（创建） 通知负责人) / `:162`(complete +50 经验)
- **一期状态**：🔵 保留 ⚠️ `related_case_id（关联案件ID）`/`parent_task_id`/`assignee_id（承办人ID）` 裸 varchar

#### Worklog（表名：worklogs（工作日志））`worklog`

- **功能**：工作日志，审批通过后回写案件人力成本、联动任务进度。
- **前置条件**：`case_id（案件ID）`、`task_id（任务ID）`（裸 varchar）。
- **后置条件**：事务内加经验 + 更新关联 Task（任务） 进度/完成 + 写 `CaseCost（案件成本）` 人力成本（时薪 200）。
- **关联功能**：注入 `Schedule（日程）`/`UserService`/`TaskService`/`CaseCost（案件成本）`/DataSource。
- **关键代码**：`worklog.service.ts:137`(approve（审批）) / `:430`(convertFromSchedule)
- **一期状态**：🔵 保留

#### Schedule（表名：schedules（日程））/ 参与人（表名：schedule_participants）`schedule`

- **功能**：日程与参与人管理。
- **前置条件**：user_id（用户ID）、orgId。
- **后置条件**：计算提醒时间；`related_case_id（关联案件ID）` 裸外键。
- **关键代码**：`schedule.service.ts:71`(createSchedule)
- **一期状态**：🔵 保留

#### MeetingRoom（会议室） / MeetingRoomBooking（表名：meeting_rooms（会议室） / meeting_room_bookings（会议室））`schedule`

- **功能**：会议室与会议室预约（冲突检测+审批）。
- **前置条件**：room_id、时间段。
- **后置条件**：冲突检测（时间相交判断）；同室其他待审自动拒绝。
- **关键代码**：`schedule.service.ts:375`(checkBookingConflict) / `:504`(approveBooking)
- **一期状态**：🔵 保留 ⚠️ 预约删除未级联；冲突检测仅覆盖 pending/approved

#### Workbench（无表，聚合）`workbench`

- **功能**：聚合当前用户待办任务、今日日程、待写/草稿日志、待审审批、承办案件。
- **前置条件**：当前登录用户。
- **后置条件**：只读聚合，无写操作。
- **关键代码**：`workbench.service.ts:39`(getSummary 多 Repository 联查)
- **一期状态**：🔵 保留

#### InternalProject（表名：internal_projects（内部项目））`internal-project`

- **功能**：内部项目（规划/进行中/完成/归档）CRUD+分页筛选。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：无业务联动（孤立表）。
- **关键代码**：`internal-project.service.ts:14`(create（创建）)
- **一期状态**：🔵 保留（manager_id 裸 varchar）

#### UpdateDynamic（无表，聚合）`update-dynamic`

- **功能**：聚合案件/合同/收款/用印/审批/工作日志的最新变更生成 feed。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：只读；六类数据源合并排序。
- **关键代码**：`update-dynamic.service.ts:30`(getFeed)
- **一期状态**：🔵 保留 ⚠️ 每类各取 limit 条再合并，可能漏跨类型时序

#### ReportTemplate（报告模板） / ReportExportLog（表名：report_templates（报告模板） / report_export_logs（报告模板））`dashboard`

- **功能**：自定义报表模板 + Excel/PDF 导出 + 定时订阅推送（@Cron 每天 9 点）。
- **前置条件**：organization_id（组织ID）、模板配置。
- **后置条件**：导出日志落 `report_export_logs（报告模板）`。
- **关键代码**：`dashboard.service.ts:1245`(report 模板) / `:1647`(@Cron 订阅推送)
- **一期状态**：🔵 保留

#### Dashboard（仪表盘） 聚合（无独立表，聚合层）`dashboard`

- **功能**：经营看板，聚合大量跨模块实体：转化漏斗、办案统计、营收/成本/净利、律师绩效、高风险预警、投诉率、财务看板、人效。
- **关键方法**：`getConversionFunnel:92` / `getCaseStats:206` / `getRevenueStats:278` / `getLawyerPerformance:337` / `getRiskAlerts:447` / **`getComplaintRateStats:556`(投诉率看板)** / `getFinanceDashboard:1062` / `getHR*:1688`。
- **投诉率看板聚合明细**：读 `complaint_tickets（投诉工单）` 按 org 拉工单 → `cases_with_complaint`=去重 case_id（案件ID） 数(`:573`)；`complaint_case_rate`=被投诉案件数/`cases（案件）` 总数(`:576`)；`complaint_amount（投诉金额）`=被投诉案件在 `receivables.contract_amount` 合计(`:581`)；另含解决率、来源分布、类型分布。
- **关联功能**：注入 `NotificationService`（高风险预警推送）；聚合 lead/case/finance/compliance/client/contract/worklog 等。
- **关键代码**：`dashboard.service.ts`（聚合方法同上）
- **一期状态**：🔵 保留 ⚠️ `getCaseTypeProfit` 用 `Math.random()` 造利润率(`:440`)；`getChannelROI` 中 cost（成本） 恒 0、roi 返回 `revenue*100`(`:199`)；多处强绑定 SQLite 函数

#### StatisticalAnalysis（无表，聚合）`statistical-analysis`

- **功能**：签约项目/金额、实收/预计收款、业务类别、团队、汇总统计（按月 strftime 聚合）。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：只读。
- **关键代码**：`statistical-analysis.service.ts:20`(query（查询） 按 type（类型） 路由) / `:160`(summaryStats)
- **一期状态**：🔵 保留 ⚠️ `payment_records（收款记录）` 无 organization_id（组织ID），只能经 `case_id（案件ID）` JOIN 过滤（跨所数据泄露风险）；强依赖 SQLite 函数

#### CallRecord（表名：call_records（通话记录））`call-records`

- **功能**：通话记录 CRUD（关联线索、录音 URL、时长）。
- **前置条件**：organization_id（组织ID）、`lead_id（线索ID）`（裸 varchar）。
- **后置条件**：仅落库。
- **关键代码**：`call-record.service.ts:14`(create（创建）) / `:29`(findByOrg)
- **一期状态**：🔵 保留（录音 URL 由调用方传入，无真实通话集成）

#### PublicOpinion（舆情） / OpinionKeyword（表名：public_opinions（舆情） / opinion_keywords（舆情））`public-opinion`

- **功能**：舆情记录 + 监控关键词配置，纯 CRUD。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：仅落库。
- **关键代码**：`public-opinion.service.ts:19`(create（创建）)
- **一期状态**：🟠 演示壳 ⚠️ 无任何爬虫/接口拉取逻辑，所有舆情需人工录入，「舆情监测」实为壳

#### ReconciliationRule（表名：reconciliation_rules（对账规则））`reconciliation-rules`

- **功能**：自动对账规则 CRUD（匹配字段 JSON、优先级、启停）。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：无（扫描范围内未发现任何消费该规则的对账执行逻辑）。
- **关键代码**：`reconciliation-rule.service.ts:14`(create（创建）)
- **一期状态**：🟡 软砍（有表无引擎，规则存了但永不运行，典型配置壳）

#### FakeLiveRoom（伪直播间） / FakeLiveMessage（伪直播消息） / FakeLiveViewer（表名：fake_live_rooms（伪直播间） / fake_live_messages（伪直播间） / fake_live_viewers（伪直播间））`fake-live`

- **功能**：直播间 CRUD、开关播、观众进出、聊天消息。本质播放一个 `video_url（视频地址）`，非真实推流。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：观众计数；聊天消息落库。
- **关键代码**：`fake-live.service.ts:38`(wxLogin 缺 env 返回 mock openid（开放ID）) / `:156`(startLive)
- **一期状态**：🟠 演示壳 ⚠️ wxLogin mock 分支默认生效；「直播」=前端循环播视频，无实时能力

#### Order（订单） / OrderItem（订单项） / Payment（支付） / VipSubscription（表名：orders（订单） / order_items（订单） / payments（订单） / vip_subscriptions（订单））`order（排序）`

- **功能**：商品订单 + 明细 + 支付记录 + VIP 订阅（月/季/半年/年卡定价）。
- **前置条件**：organization_id（组织ID）、商品信息。
- **后置条件**：事务建单+明细；订阅自动开并顺延。
- **关键代码**：`order.service.ts:59`(createOrder) / `:157`(payOrder) / `:215`(subscribeVip)
- **一期状态**：🔵 保留 ⚠️ `payOrder` 注释「模拟支付成功」，payment.status 直接写 SUCCESS，无真实支付网关；订单与案件/客户无关联（孤立电商表）

#### CooperativeFirm（合作律所） / CooperativeSource（合作来源） / DifficultCase（表名：cooperative_firms（合作律所） / cooperative_sources（合作律所） / difficult_cases（合作律所））`shortcut`

- **功能**：协作律所、协作案源、疑难案件库（含讨论/方案 JSON 记录）。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：无跨模块联动（三表均与案件无外键关联）。
- **关键代码**：`cooperative-firm.service.ts:72` / `difficult-case.service.ts:91`(addDiscussion/AddSolution 走 JSON 日志)
- **一期状态**：🔵 保留（organization_id（组织ID） 裸 varchar）— 注意：该模块一期已被「整组软砍」（菜单不可见）

#### LawyerRating（表名：lawyer_ratings（律师评分））`lawyer-center`

- **功能**：律师列表/主页聚合 + 评级管理（综合分、维度 JSON）。
- **前置条件**：organization_id（组织ID）、user_id（用户ID）。
- **后置条件**：评级落库。
- **关键代码**：`lawyer-center.service.ts:20`(findLawyers) / `:152`(createRating)
- **一期状态**：🔵 保留 ⚠️ `findLawyers` 用 `FIELD_OPTIONS[Math.abs(u.id.charCodeAt(0)) % len]` 按 ID 字符码伪随机选专业领域，非真实数据（一期已被整组软砍）

#### Mail（表名：mails（邮件））`mail`

- **功能**：内部邮件（收件箱/已发/草稿/垃圾），收件人按 JSON 数组存。
- **前置条件**：sender_id、recipient_ids。
- **后置条件**：给每个收件人建 inbox+给发件人建 sent。
- **关键代码**：`mail.service.ts:14`(send) / `:83`(findInbox 用 LIKE %"userId"%)
- **一期状态**：🔵 保留 ⚠️ recipient_ids 为 JSON+LIKE 模糊匹配，多收件人易误命中；无真实邮件协议（仅站内信）

#### PushRule（表名：push_rules（推送规则））`push-rule`

- **功能**：案件节点（立案/开庭/结案…）推送开关 + 内容模板 + 渠道配置。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：无（扫描范围内未发现案件模块消费该规则触发推送的代码）。
- **关键代码**：`push-rule.service.ts:39`(findRules) / `:55`(updateRule 不存在则建默认)
- **一期状态**：🔵 保留 ⚠️ 疑似配置壳（消费者待核查，建议核实 case 模块是否引用 PushRuleService）

#### Sms（无独立实体，真实对接）`sms（短信）`

- **功能**：`sendCaseSms` 在 20 类案件节点触发 C 端短信，模板 ID 从 `CHUANGLAN_TMPL_*` 环境变量读取，发送前写 `case_push_notifications（案件推送通知）`(channel（渠道）='sms（短信）')。
- **前置条件**：`CHUANGLAN_ACCOUNT/PASSWORD` 及模板已配置。
- **后置条件**：未配置或模板时静默跳过返回 false（仅留推送记录）。
- **关联功能**：注入 Case/User/CasePushNotification（实体在 client 模块）；导出供案件模块触发。
- **关键代码**：`sms.service.ts:186`(sendCaseSms) / `sms.client.ts:70`(真实 POST smssh.253.com)
- **一期状态**：🔵 保留（真实创蓝 253 短信接口）

#### Comprehensive（无表，聚合）`comprehensive`

- **功能**：综合查询入口（已立项/已报备/已签约/已批文档/合同约定收款/已开票未收/审批）。
- **前置条件**：organization_id（组织ID）、查询类型。
- **后置条件**：只读。
- **关键代码**：`comprehensive.service.ts:32`(query（查询） 按 type（类型） 路由)
- **一期状态**：🔵 保留 ⚠️ `biz-established` 分支含无效 TypeORM 写法（被特判分支覆盖，常规 path 不生效，属死代码）

#### Diagram（表名：diagrams（图表））`diagram`

- **功能**：思维导图/流程图/法律关系图/组织架构图，`content（内容）` 存 `{nodes（节点）,edges}` JSON。
- **前置条件**：organization_id（组织ID）。
- **后置条件**：图数据完全前端构造，后端仅存储。
- **关键代码**：`diagram.service.ts:25`(create（创建） 默认空图)
- **一期状态**：🔵 保留（一期已整组软砍）

---

## 6. 数据模型要点

### 6.1 主干实体（表名）

- **Organization（组织）** (`organizations（组织）`) — 租户根，~50 个实体 @ManyToOne 引用
- **User（用户）** (`users（用户）`) — 操作人/负责人，字段 `role（角色） varchar`（`user.entity.ts:26`）
- **Role（角色） / Permission（权限） / Menu（菜单）** (`roles（角色）`/`permissions（权限）`/`menus（菜单）`) — RBAC（无关联表，`Role.permissions:string[]` 自由 JSON）
- **Lead（线索）** (`leads（线索）`) — 线索，`stage/status` 见 §6.2
- **Case（案件）** (`cases（案件）`) — 案件，关键裸列 `client_id（客户ID） / lead_id（线索ID） / contract_id（合同ID）`（无 @ManyToOne）
- **Contract（合同）** (`contracts（合同）`) — 合同，`stage（阶段）` 裸 varchar，`template_id（模板ID）` 关联模板，`case_id（案件ID）` 裸列
- **Receivable（应收）** (`receivables（应收）`) — 应收，`status（状态）` pending/partial/completed/overdue，`case_id（案件ID）` 裸列
- **ComplaintTicket（投诉工单）** (`complaint_tickets（投诉工单）`) — 投诉工单（C 端与 B 端统一来源）
- **CommissionRecord（佣金记录）** (`commission_records（佣金记录）`) — 分润记录
- **BusinessFund（业务款）** (`business_funds（业务款）`) — 业务款/收支总账（案件成本、收款都同步到此）
- **CaseCost（案件成本）** (`case_costs（案件成本）`) — 案件成本，create/update 时同步写 BusinessFund（业务款）

### 6.2 关键状态枚举（定义位置）

| 概念   | 枚举                                  | 值                                                                                                                                   | 位置                              |
| ---- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| 用户角色 | `UserRole`                          | super_admin / org_admin / marketing / sales / lawyer / assistant / finance / client                                                 | `types/index.ts:1`              |
| 案件状态 | `CaseStatus`                        | pending_assign / processing（处理中） / filing / evidence / hearing / appeal / pending_close / closed（已关闭） / terminated / voided         | `types/index.ts:38`             |
| 案件阶段 | `case.stage`（**裸 varchar，无枚举**）     | intake / processing（处理中） / closing（结案） / closed（已关闭）                                                                                | `case.entity.ts:269`            |
| 线索状态 | `LeadStatus`                        | new / pending_follow / following / inviting / negotiating / pending_sign / lost                                                     | `types/index.ts:20`             |
| 合同阶段 | `contract.stage`（**裸 varchar，无枚举**） | drafting / reviewing / **signing(待签)** / **signed(已签)** / performing / completed（已完成） / terminated / voided / **case_failed(隐式扩展)** | `contract.entity.ts:47`         |
| 应收状态 | `ReceivableStatus`                  | pending（待处理） / partial（部分收款） / completed（已完成） / overdue（逾期）                                                                         | `receivable.entity.ts:4`        |
| 投诉工单 | `TicketStatus`                      | pending（待处理） / processing（处理中） / resolved（已解决） / closed（已关闭） / escalated（已升级）                                                       | `complaint-ticket.entity.ts:41` |
| 分润状态 | `CommissionStatus`                  | pending（待处理） / paid（已付）                                                                                                             | `commission-record.entity.ts:8` |
| 发票状态 | `InvoiceStatus`                     | pending（待处理） / issued / paid（已付） / cancelled（已取消）                                                                                   | `invoice.entity.ts:3`           |

> ⚠️ **枚举分散风险**：同一概念在多处各自定义（后端 `types/index.ts` + 各 entity 内 `enum`/裸 varchar + 前端常量），存在漂移。例如 `Case.stage` 后端是裸 varchar、前端却有一套 stage（阶段） 文案映射；`ComplaintStatus`（`types/index.ts:80`）与 `TicketStatus` 并存易混淆。`contract.stage` 的 `signing（待签）`/`case_failed` 为 fadada 隐式扩展值，不在 `ContractStageEnum` 内。

### 6.3 外键 vs 裸列（数据一致性风险面）

- ✅ **有 @ManyToOne 真外键**：Case（案件）→Organization/User；Lead（线索）→Organization/User；Receivable/Fee/Refund/ProfitShare/CaseCost→Case（案件）；Contract（合同）→Organization（组织）；ComplaintTicket（投诉工单）→Case/User/Organization。
- ⚠️ **裸 xx_id 列、无 @ManyToOne（删除主表会成孤儿）**：
  1. `Receivable.case_id` / `Invoice.case_id` / `Contract.case_id`(`contract.entity.ts:26`)
  2. `Case.client_id`(`case.entity.ts:24`) / `Case.lead_id` / `Case.contract_id`
  3. `Lead.case_id` / `Contract.lead_lawyer_id` / `Contract.approver_id`
  4. **ClientProfile（客户）在关系图里完全孤立**——没有任何实体 @ManyToOne 引用它，仅 Case.client_id 等裸列关联
- 建议：对上述 1/2/3 类补充 @ManyToOne 或应用层级联清理，否则案件删除后应收/发票/合同可能成为脏数据。
- ⚠️ **数字资产类实体普遍裸外键**：task/worklog/schedule/shortcut/mail 的 `case_id（案件ID）`/`related_case_id（关联案件ID）`/`client_id（客户ID）` 等均为 varchar 裸列。
- ⚠️ **跨所数据泄露风险**：`payment_records（收款记录）`/`fees（费用）` 等表无 `organization_id（组织ID）`，聚合统计只能经 `case_id（案件ID）` JOIN 过滤，若 JOIN 遗漏即串所。

---

## 7. 核心业务链路

> 链路通畅性基于当前代码；标注「⚠ 断点」的是已知问题（详见 §15）。

### 7.1 链路一：获客 → 线索（通畅）

落地页 webhook（回调钩子） `ad-platforms/webhook.controller.ts:44` / 活码 `scrm/live-code.controller.ts:7` → Lead（线索） 写入（`lead.controller.ts:7`）。线索经 公海 `lead-pool` → 分配 `lead-assignments` → 邀约 `invite-tasks` → 商机 `opportunities（商机）` → 谈案 `talk-sop`。

- ⚠ 活码 `ChannelTracking（渠道追踪）` 不自动建 Lead（归因丢失）；邀约无外呼/短信（仅站内通知）；线索再分配 `reassignLead` 无前端路由。

### 7.2 链路二：发合同 → 建案 → 利冲/应收/分润（通畅，分润已修）

1. 发合同：`contract.controller.ts:94 @Put(':id/sign')` → `fadada.service.ts:1416 createSignTaskFromTemplate`（写 `signingCompliance`）
2. 客户签署 → 回调 `fadada.controller.ts:19 @Post('callback（回调）')` → `fadada.service.ts:1627 handleCallback` → `sign-task-finished` → `generateCaseFromContract`(`fadada.service.ts:1703`)
3. **建案 + 应收 = 同一 DB 事务**（`fadada.service.ts:1559-1573`，已修复原「无事务」缺口）
4. 利冲 `conflictCheckService.check`(`fadada.service.ts:1579`)：命中即挂 `conflict_hold`，异常也挂起+强告警（已修，不再静默）
5. 分润：`fadada.service.ts:1598` 原签约处死代码**已删除**；✅ 实际由「结案(`case.service.ts:766`) / 全款到账(`finance.service.ts:202`)」触发，条件 `status（状态）=CLOSED && 应收 COMPLETED`（`commission.service.ts:303`）

### 7.3 链路三：办案（成本同步✓，SOP任务生成是死代码）

`case.controller.ts:13 cases（案件）` → 办案 SOP / `case-task.controller.ts` / 文书 `legal-document.controller.ts` / 预警 `case-warnings`。

- ✅ 案件成本 `case-cost.service.ts:98 syncCostToFinance` 同步写 `BusinessFund（业务款）`，进入财务台账
- ⚠ `case-task.service.ts:22 generateTasksFromTemplate` **全库无调用方**（办案 SOP 任务生成是死代码），任务实际靠手动
- ⚠ 文书 `legal-document` 为模板拼接（非真 AI，见 §15 外壳清单）
- ⚠ 结案触发分润/评价的代码路径 `catch` 静默（`case.service.ts:767/771`）

### 7.4 链路四：财务（收款→分润✓，代扣无端点）

收款 `finance.controller.ts:259 record-payment`（事务内回写应收，completed（已完成）→触发分润 `finance.service.ts:202`）→ 分润 `commission（分润校验）` → 对账 `reconciliation.controller.ts:7` → 发票 `finance.controller.ts:185/429` → 退费 `:113` → 单案利润 `finance-statement.controller.ts:7`。

- ⚠ **代扣无 controller 端点**（仅有实体 `withholding-batch`/`tax-calculation`），本期未暴露 API
- ⚠ 发票/退费/代扣为状态壳，未接真实税控/银行网关
- ⚠ 收款未同步写 BusinessFund（仅案件成本同步了），利润失真

### 7.5 链路五：合规 / 投诉（同步存在但失败静默）

C 端提交 `client.service.ts:194 createComplaint` 写 `complaint（投诉）` 并同步生成 `complaint_tickets(:214)`；处理 `compliance.controller.ts:23`；投诉率看板 `dashboard.controller.ts:94 → dashboard.service.ts:556 getComplaintRateStats`（被投诉案件率 / 投诉金额 / 解决率 / 来源类型分布）。

- ⚠ C 端工单同步失败仅 `console.error(:226)` 静默——客户以为投诉成功、实际没进 B 端

### 7.6 链路六：C 端回流（支付本期不做）

签约预填 `generateCaseFromContract` 读 `case_supplement（审查意见）`；投诉/归档/案件查看均有接口。**支付本期不做**（无 client 支付端点，仅 mock-fadada）。

### 7.7 核心链路一句话总览

线索 → `fadada.launchSignFromLead` 发签(`contracts.stage='signing（待签）'`) → 法大大回调 `handleCallback` → `generateCaseFromContract` 建案+建应收+利冲 → 合同 `stage（阶段）='signed（已签）'`；合同 `approve（审批）` 自动派生用印申请 → `seal.useApplication` 盖章回写合同 `signed（已签）`；案件 `closeCase/archiveCase` 汇总应收触发 `commissions` 并写 `archive_no（案件大类）`（但**不**自动建 `archive_volumes（归档卷宗）`）。利冲为合规红线，命中即 `conflict_hold`。

---

## 8. 前端架构

### 8.1 技术栈

- 构建：**Vite 6**（`frontend/package.json:7`），`dev`=vite、`build`=`tsc -b && vite build`
- 框架：**React 19.1** + TypeScript 5.8（strict）
- UI：**Ant Design 6.5.1** + `@ant-design/icons` + recharts + @dnd-kit
- 路由：react-router-dom 7（BrowserRouter，全 `React.lazy` 懒加载）
- 请求：axios 1.18，**单例** `baseURL:'/api'`（`src/api/axios.ts:6-9`）
- 状态管理：**无 Redux/Zustand**，登录态存 `localStorage`，菜单权限靠 `Layout.tsx` 角色矩阵

### 8.2 路由与守卫（`App.tsx`）

- 所有页面 `lazy(()=>import(...))`（`App.tsx:12-74`）
- `ProtectedRoute`（`App.tsx:246-266`）：无 user→`/login`；`role（角色）==='client'`→`/client`；配 `allowedRoles` 不符→跳角色首页
- `ClientProtectedRoute`（`App.tsx:277-285`）：C 端登录态校验

### 8.3 菜单与权限（`components/Layout.tsx`）

菜单完全由两套角色矩阵驱动：

- `menuGroups`（`Layout.tsx:39-227`）：11 个分组 + 所有菜单项
- `roleGroupAccess`（`Layout.tsx:231-245`）：角色 → 可见分组
- `roleSubMenuAccess`（`Layout.tsx:249-390`）：分组 → 子项可见角色
- 过滤逻辑 `filteredMenuGroups`（`Layout.tsx:400-416`）：先过滤分组、再过滤子项、空分组移除

### 8.4 API 层（`src/api/`，72 个文件）

- 请求拦截器注入 Bearer token（`axios.ts:11-25`）
- 响应拦截器直接 `return response.data`，401 清登录态跳登录（区分 B 端 `/login` 与 C 端 `/client/login`）
- 每个文件对应一个后端模块（`auth.ts`/`case.ts`/`client.ts`/`lead.ts`/`contract.ts`/`finance.ts`/`compliance.ts` 等）

### 8.5 页面清单（按模块）

- **数据看板**：Dashboard(完整)、OverviewManagement(空壳)、转化漏斗/销售绩效/办案效能/合规风险
- **线索 CRM**：LeadManagement、ClientManagement、LeadPool（线索池）、InviteWorkbench、TalkWorkbench、TalkSOPConfig、TalkQualityCheck（话术质检）、ClientTagManagement
- **案件办案**：CaseManagement(52K)、CaseDetail(77K)、CaseSOPConfig、CaseWarningCenter、ContractManagement(62K)、PropertyPreservation（财产保全）、ConflictCheck（利益冲突审查）、ComplaintManagement、DocumentManagement/Seal/Preview、ArchiveManagement(空壳)、ArchiveVolume（归档卷宗）、CloudArchive、ChatArchive（聊天存档）
- **财务分润**：FinanceManagement/Operation/Dashboard/Report、AccountSettlement、CommissionConfig、Reconciliation（对账）、RefundManagement、IncomeExpenditure、BusinessFund（业务款）、电子/纸质发票打印、BatchWithholding、FixedCost/IncomeTax Withholding、OrderManagement
- **投放营销**：AdAccount/AdPlan(33K)/AdPlatform、ConversionReport、ChannelTracking（渠道追踪）、AIContentGenerator、SocialAccountMatrix、DigitalHumanLive（数字人直播）、FakeLiveManagement、LiveCode（活码）、ReachTool、PushRuleConfig、ContentPreviewWorkbench、PublicOpinionMonitor
- **合规风控**：ComplianceCenter(22K)、ComplianceExport、ComplianceManagement(空壳)、ComplianceRiskDashboard、ConflictCheck（利益冲突审查）、DueDiligenceTool、AuditLogManagement、NumberRuleConfig、MenuManagement、PermissionManagement、OrganizationManagement
- **系统/人事行政**：UserManagement、PersonnelManagement、AttendanceManagement(空壳)、HRMaterial、ActivityManagement、HRDashboard、Notification、DeploymentConfig（部署配置）、BrandCustomization、DiagramTool、ApprovalCenter、Seal（用印）、BidManagement、CooperativeFirm/Source、DifficultCases（疑难案件）、CustomReport、ProfitModelSimulator、ComprehensiveQuery、ColleagueCircle、AINavigation、AIContentGenerator

### 8.6 C 端（`pages/client/` + `BottomNav`）

- BottomNav 4 项：首页 `/client`、案件 `/client/cases`、服务 `/client/service-hall`、我的 `/client/profile`
- 页面：ClientLogin/Home/CaseList/CaseDetail/Profile/Notifications/Payment/Complaint/MyComplaints/ServiceRating/SignPrefill/MockFadada
- 已软砍（页面仍在但不在导航）：AIConsult、ClientArchive（客户归档）、ClientHelpCenter

---

## 9. API 约定

| 项      | 约定                                                                                                                                 |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| 全局前缀   | `/api`（`main.ts:48`）                                                                                                               |
| 认证     | Bearer JWT（`Authorization` 头）；`@UseGuards(JwtAuthGuard)` + 全局 `RolesGuard`                                                         |
| 角色装饰器  | `@Roles('super_admin','finance',...)`（`auth/roles.decorator.ts:6`）                                                                 |
| 分页     | **不统一**：多数 `page（页码） + limit`；少数 `pageNum/pageSize`；返回 `{data,total（合计）}` 或 `{list（列表）,total（合计）,page（页码）,pageSize}`。**无统一分页 DTO** |
| 统一响应包装 | **不存在**（无 `ResponseInterceptor`）；各 Controller 自定，常见 `{success,data,total（合计）}` / `{data,total（合计）}`                                |
| 错误处理   | `HttpException` 子类；`AllExceptionsFilter`(`main.ts:9-28`) 响应体 `{statusCode,message（消息）}`，**无业务 code（编码） 字段**                        |
| 输入校验   | 全局 `ValidationPipe({whitelist, forbidNonWhitelisted, transform})`；DTO 用 class-validator                                            |
| 限流     | 全局 Throttler 100 次/60 秒（`app.module.ts:70-72`）                                                                                     |

**主要模块 base path**：`auth` / `leads（线索）` / `lead-pool` / `lead-assignments` / `opportunities（商机）` / `invite-tasks` / `talk-sop` / `contracts（合同）` / `fadada`(含 `/callback`) / `sign-template` / `cases（案件）` / `case-tasks` / `case-warnings` / `legal-documents` / `conflict-checks` / `finance`(`/fee`/`/refund`/`/invoice`/`/receivables/:id/record-payment`/`/business-funds`/`/statements`/`/commission`/`/case-costs`) / `compliance（合规）` / `client`(C 端) / `client-profiles` / `dashboard` / `marketing` / `scrm/*` / `ad-*` / `social-*` / `hr`

> 前端调用约定：统一走 `/api`，靠 `vite.config.ts` 代理到后端。

---

## 10. 本地开发与运行

### 10.1 安装依赖

```bash
cd "/Users/season/AI编程/法律咨询/全链产品/backend" && npm install
cd "../frontend" && npm install
```

> 可用 npmmirror：`npm install --registry=https://registry.npmmirror.com`

### 10.2 启动后端

```bash
cd backend && npm run start:test    # 开发：0.0.0.0:3000，前缀 /api
```

- 无需手动建库：SQLite + `synchronize` 自动建表
- **自动 seed**：`SeedsModule`（`seeds/seeds.module.ts:267` `OnModuleInit`）随启动执行
  - 开发库：建「测试律所」+ **12 个演示账号，密码统一 `123456`**（`:517-536`）
  - 超管示例：`13800138000 / 123456`；C 端示例：`13800138007 / 123456`
- ⚠ 必须在 `backend/` 目录内启动（sqlite 路径相对 cwd）

### 10.3 启动前端（另开终端）

```bash
cd frontend && npm run dev:test     # http://localhost:5173 → /api 代理 3000
```

- 端口 `strictPort:true`，占用即报错
- 生产构建 `npm run build` → `frontend/dist/`

### 10.4 重置数据

删除 `backend/fazhihui.sqlite`（开发库），重启后端会重新 `synchronize` + seed。

---

## 11. 配置项说明

`backend/.env`（**不入库，无 `.env.example`**，新环境需手工创建）：

| 键                                                                                                                                                                                  | 用途                                         |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| `PORT`                                                                                                                                                                             | 监听端口（默认 3000）                              |
| `JWT_SECRET` / `JWT_EXPIRES_IN` / `BCRYPT_ROUNDS`                                                                                                                                  | 令牌签名/有效期/密码哈希轮数                            |
| `DB_*` (DB_HOST/PORT/USER/PASSWORD/DATABASE)                                                                                                                                       | ⚠ **Postgres 遗留项，代码已完全不引用**，实际用 SQLite，可忽略 |
| `FADADA_ENABLED` / `FADADA_MODE`(mock/uat/prod) / `FADADA_APP_ID` / `FADADA_APP_SECRET` / `FADADA_API_URL` / `FADADA_REDIRECT_URL` / `FADADA_CALLBACK_TOKEN` / `FADADA_NOTIFY_URL` | 法大大电子签（本地可 `FADADA_MODE=mock`）             |
| `CHUANGLAN_*`                                                                                                                                                                      | 创蓝短信（案件节点提醒模板）                             |
| `WX_MINI_APPID` / `WX_MINI_SECRET`                                                                                                                                                 | 微信小程序                                      |
| `CORS_ORIGIN`（在 `.env.development`/`.env.production`）                                                                                                                              | 逗号分隔白名单；未配则默认放行 localhost:5173/5174/8080   |

> 前端 `frontend/` 下**无 `.env`**，所有联调靠 `vite.config.ts` 的 `/api` 代理。

---

## 12. 部署方式

**裸机 PM2 + Nginx，无 Docker。** 目标：腾讯云 Ubuntu 22.04，`/home/ubuntu/fazhihui`。

1. `deploy/install-env.sh`：装 Node 20、npmmirror、PM2、Nginx、Git，配开机自启
2. `deploy/deploy.sh`：校验环境 → 装前后端依赖 → `npm run build` ×2 → `pm2 start ecosystem.config.js && pm2 save（保存）` → 拷贝 `deploy/nginx/fazhihui.conf` → `nginx -t && reload` → curl 健康检查
3. `backend/ecosystem.config.js`：双实例同机 —— `fazhihui-prod`(prod,3001) / `fazhihui-test`(dev,3000)，`max_memory_restart:500M`，日志 `backend/logs/`
4. `deploy/nginx/fazhihui.conf`：`test.meichuangmenye.com→3000`、`fazhihui.meichuangmenye.com→3001`，SPA 回退 `try_files $uri $uri/ /index.html`，`client_max_body_size 20m`，HTTPS 块待启用
5. `deploy/deploy-fadada-uat.sh`：单点热更法大大 UAT 配置
6. 日常更新：`cd /home/ubuntu/fazhihui && git pull && bash deploy/deploy.sh`

> 根 `sync.sh` 仅是 Git 同步器（自动 commit+push（推送）），不做部署。

---

## 13. 多端壳子

全部是「原生壳 + WebView 套 H5」，**同仓库、非独立仓库**，改 H5 无需重新发版：

| 目录                            | 形态                         | H5 入口常量                                                                          |
| ----------------------------- | -------------------------- | -------------------------------------------------------------------------------- |
| `android-client-shell/`       | Android 原生 WebView（Gradle） | `MainActivity.java:57` `CLIENT_URL=https://test.meichuangmenye.com/client/login` |
| `ios-client-shell/`           | SwiftPM 原生壳（iOS 14+）       | `AppConfig.swift:6` `clientURL`                                                  |
| `mini-program-webview-shell/` | 微信小程序 web-view 壳           | `app.js:6` `h5BaseUrl=.../client`                                                |
| `mini-program/`               | 早期原生小程序（直播场景）              | 独立实现，未内嵌 H5                                                                      |
| `web-live/`                   | 独立直播 H5 页                  | 与主工程解耦，`cors-server.py` 本地静态服务                                                   |

三端壳子共用 `frontend` 的 `/client` 路由（`src/api/axios.ts:45` 有 `/^\/client(\/|$)/` 判断）。

---

## 14. 第一期上线范围与菜单收口

依据 PRD 第十二章三期规划，**一期仅上核心业务闭环 + 必要配置**，其余软砍（隐藏菜单，代码保留、URL 仍可访问、可逆）。

**一期保留**：线索闭环（线索/公海/邀约/谈案/谈案SOP/客户）、案件办案（案件/办案SOP/预警/AI文书/类案/合同/保全/利冲/归档/文档）、财务（经营/收款台账/收支/分润/对账/退费/利润/催款/发票/业务款等）、合规（**仅投诉管理**）、投放（仅投放岗核心：账户/平台/计划/转化/素材）、系统（用户/权限/菜单/组织/推送/编号/通知/审计等）、人事（**仅审批中心+用印**）。

**已软砍（不可见，代码仍在）**：

- 整组隐藏：`comprehensive` / `shortcut` / `lawyer-center`（`Layout.tsx:173-205`，已从 `roleGroupAccess` 移除）
- 组内剔除：自定义报表/人效/盈利模型、6 个 SCRM、投标/尽调宝、合规管理/舆情、4 个代扣、营销 5 个演示项（AI内容/数字人/伪直播/工作手机/内容预审）、AI工具、人事行政 8 项（考勤/日志/日程/任务/知识库/绘图等）

**执行方式**：仅改 `frontend/src/components/Layout.tsx` 的菜单矩阵，未删页面代码、未改业务逻辑。恢复只需加回对应分组/子项。

> 另：已物理删除 19 个死代码页（`Stitch*`×10、`LawyerGrade*`×5、Calculator/Timer/Mail/SocialCircle），`git rm` 保留历史可恢复。

---

## 15. 已知技术债与待办

按优先级（P0 上线前必修 / P1 数据治理 / P2 需外部服务）：

### P0 — 上线前必修（纯代码可补）

1. **RBAC 免鉴权**：`roles.guard.ts:36-38` 未加 `@Roles（角色）` 即放行 → 漏装饰器即匿名越权。建议改为 deny-by-default。
2. **静默吞错致账实/数据不符**：
   - 分润触发失败被 `catch` 静默（`finance.service.ts:204`、`case.service.ts:767`）
   - C 端投诉工单同步失败仅 `console.error`（`client.service.ts:226`）
   - 财产保全回写 `risk_notes（风险说明）` catch 全空（`property-preservation.service.ts:241/286`）
   - 审计失败不影响主流程（`audit.service.ts:33`）
   - 建议：静默处改为强告警 + 可观测（审计/通知）
3. **分润口径**：`commission.service.ts:252` 仅算主办律师（协办 TODO），需确认是否补齐
4. **收款不写业务款总账**：`finance.service.ts:185` 回写应收但未同步 `BusinessFund（业务款）`，利润失真（案件成本同步了、收款没同步）

### P1 — 数据治理（1–2 周）

1. 补 @ManyToOne 外键（§6.3 的 1/2/3 类裸列），或应用层级联清理
2. 统一三套利润口径、移除 `dashboard.service.ts:440` 附近的 `Math.random()` 假数据
3. 统一分页 DTO 与响应包装（当前混乱，前端需按 HTTP status（状态） 判断）
4. `generateTasksFromTemplate` 补调用方，让办案 SOP 真正实例化任务
5. 补齐审计覆盖（投诉/归档/签约/支付目前漏审计）
6. 抽 `shared-kernel`，降低 Organization/User/Case 跨边界强耦合
7. **审批孤岛修复**：`ApprovalModule` 未导出 `ApprovalService`、`approval_flows（审批流）` 未被读取、审批结果不回写 `target_id（目标ID）`——需打通 seal/contract/finance 等业务流
8. **Dashboard（仪表盘） 指标错位**：`fees（费用）` 表已弃用、`dashboard` 仍按 `Fee.paid` 统计，应改读 `business_funds（业务款）`
9. **跨所数据泄露**：`payment_records（收款记录）` 无 `organization_id（组织ID）`，聚合统计需严格经 `case_id（案件ID）` JOIN 过滤

### P2 — 需外部服务（二/三期）

- 真 AI 文书/咨询（当前模板拼接）
- 真发票税控、真代扣/退费银行网关、真舆情采集、真数字人/直播
- 外呼/短信（邀约当前仅站内通知）
- 投放真实归因（活码不建 Lead（线索））
- 真支付网关（order（排序） `payOrder` 模拟成功；C 端支付本期不做）

### 外壳/Mock 清单（对外演示可用，但别写入合同承诺"系统自动做"）

AI 文书/AI咨询、发票税控、代扣/退费网关、舆情监控、数字人直播、伪直播、内容生成、营销归因、尽调宝报告、法大大深层利冲工商数据、`RiskDisclosure（风险告知）`/`ContractTemplate（合同模板）`/`LawyerQualification（律师资质）` 三张孤儿实体。

---

## 16. 角色与权限模型

- 角色枚举 `UserRole`：`super_admin`(超管) / `org_admin`(律所管理者) / `marketing`(投放) / `sales`(销售) / `lawyer`(律师) / `assistant`(助理) / `finance`(财务) / `client`(C 端客户)
- RBAC 实体在 `user` 模块：`Role.permissions:string[]`（自由 JSON，无权限关联表）、`Menu（菜单）`（菜单树）
- 鉴权链：`JwtAuthGuard`（Bearer 校验）→ 全局 `RolesGuard`（读 `@Roles（角色）`，无装饰器即放行，`super_admin` 跳过校验，不匹配 403）
- 菜单权限：`Layout.tsx` 的 `roleGroupAccess` + `roleSubMenuAccess` 两套矩阵（见 §8.3）
- ⚠ **RBAC 实际未生效**：`roles.permissions`、`menus.permissions`、`permissions（权限）` 表均为 JSON/字典数据，`RolesGuard` 只认 `users.role` 枚举，RBAC 是「配置孤岛」，未接入运行时鉴权；运行时改角色需重新登录才生效（角色取自 JWT 载荷而非 DB）。
- ⚠ 因 `roles.guard` 默认放行，**任何漏加 `@Roles（角色）` 的接口都等价于公开**，新增接口务必加装饰器。

---

## 17. 附：最短上手路径

```bash
# 1. 安装
cd "/Users/season/AI编程/法律咨询/全链产品/backend" && npm install
cd ../frontend && npm install

# 2. 后端（自动建表 + 自动 seed，无需额外命令）
cd ../backend && npm run start:test        # 0.0.0.0:3000 /api

# 3. 前端（另开终端）
cd ../frontend && npm run dev:test         # localhost:5173 → 代理 3000

# 4. 登录
#    管理端：13800138000 / 123456
#    C 端：  13800138007 / 123456 → /client
```

> 改完后端记得 `npm run build`/重启；前端改菜单在 `Layout.tsx`、改业务逻辑在 `pages/`。  
> 数据库文件 `backend/fazhihui.sqlite` 不入库，重置即删除后重启。

---

### 实体详解速查索引

| 关注点                | 去看的章节                                                                  |
| ------------------ | ---------------------------------------------------------------------- |
| 想看某实体的「做什么/前后置/关联」 | 第 5 章各域卡片                                                              |
| 核心签约→建案链路          | §7.2 + §5.3 的 Contract(fadada)/ConflictCheck/Case/Receivable/Seal      |
| 财务资金流              | §5.4 的 BusinessFund/Receivable/PaymentRecord/CommissionRecord/CaseCost |
| 投诉与合规              | §5.3 的 ComplaintTicket/SalesCompliance/TalkQualityCheck + §7.5         |
| 哪些是演示壳/死代码         | 各卡片「一期状态」标记（🟠/⚫）+ §15 外壳清单                                            |
| 数据一致性坑             | §6.3 裸外键清单 + §15 P1                                                    |
